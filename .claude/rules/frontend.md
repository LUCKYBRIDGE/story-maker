# Frontend work

For UI changes, follow the relevant design tokens and `.agents/rules/ui-design.md`. Use the affected flow and checks required by AGENTS.md; do not add a separate unconditional build or full-device gate.
