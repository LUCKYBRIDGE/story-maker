# Claude Frontend Workflow Rules
- Always verify changes with `npm run build` or project test suite.
- Follow tokens in `docs/design/tokens.md`.
- Keep changes surgical and preserve working features.
