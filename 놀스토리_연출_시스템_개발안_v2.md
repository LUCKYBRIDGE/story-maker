# 놀스토리 연출 시스템 개발안 v2

> 대상: `LUCKYBRIDGE/story-maker`
> 기준: 2026-09-17 현재 확인한 `main` 구조
> 목적: 기존 놀스토리 편집·재생·파일·스프레드시트 구조를 해치지 않으면서, Pinky 계열 작품에서 사용하던 고급 시각 연출을 놀스토리의 일반 기능으로 승격한다.

---

## 0. 최종 설계 결정 요약

이번 개발안은 다음을 **명시적으로 결정**한다.

1. `StoryLine.effect` 중심 구조를 `StoryLine.presentation` 중심 구조로 교체한다.
2. Presentation은 `effects / look / transition / actors` 네 영역으로 분리한다.
3. **순간 효과와 지속 분위기를 같은 개념으로 취급하지 않는다.**
4. 지속 분위기(`look`)는 런타임의 암묵적 상속 상태로 저장하지 않는다. **각 컷에 최종 상태를 명시적으로 저장한다.**
5. 사용자가 보는 `몇 컷 동안`, `계속 적용`은 **데이터의 duration 속성이 아니라 편집기의 범위 적용 명령**으로 구현한다.
6. 이 방식으로 선택지·goto·이전 컷·이어읽기·임의 컷 미리보기에서도 화면 상태를 결정적으로 유지한다.
7. Actor Staging은 현재 자동 인물 레이아웃을 대체하지 않고 **그 위에 적용되는 저강도 author override**로 만든다.
8. Transition은 단순 시각 레이어가 아니라 `StoryPlayer` 진행 제어와 연결한다.
9. StoryDocument 내부 스키마는 v2로 올리고 v1을 명시적으로 migration한다. `.knolstory` 외부 container manifest는 구조가 바뀌지 않는 한 v1을 유지한다.
10. Google Sheets/Excel은 **이번 버전에서는 기존 8개 탭을 유지**한다. 다만 8탭 자체를 영구 계약으로 만들지는 않는다.
11. 고급 연출 정보는 `컷 대본`의 숨김/고급 보존 열에서 lossless round-trip을 보장한다.
12. Pinky 연출은 마지막 단계에서 `PinkyPresentationAdapter`를 통해 일반 `StoryPresentation`으로 변환한다. Pinky 전용 런타임 코드는 두지 않는다.
13. 편집기 기본 화면은 기존 놀스토리 구조를 유지한다. 연출 기능 때문에 3열 전문 영상 편집기처럼 재설계하지 않는다.
14. 기본 사용은 1~2번 클릭, 복잡한 조정은 Progressive Disclosure로 감춘다.

---

# 1. 현재 구조에서 지켜야 할 것

현재 놀스토리는 이미 다음 기반을 갖고 있다.

- 컷 중심 `StoryLine`
- `StorySceneFrame`을 통한 편집 미리보기/실제 플레이 화면 공유
- `StoryLine.effect?: StorySceneEffect`
- 기존 효과 registry 성격의 `STORY_EFFECTS`
- `SceneEffectEditor`
- `useSceneEffect`
- Web Animations API 기반 효과와 cleanup
- `prefers-reduced-motion`
- `goto`와 2~3개 선택지를 가진 `StoryFlow`
- 실제 플레이 경로를 기록하는 history
- 자동/수동 이어읽기 저장
- Google Sheets / Excel 공통 import 흐름
- `.knolstory` / `.nolstory` 파일
- strict StoryDocument validation
- 공유 / remix / publication fingerprint
- 이미지 알파 영역과 화면 크기를 고려하는 기존 인물 자동 레이아웃

따라서 이번 작업은 새로운 게임 엔진을 만드는 작업이 아니다.

현재 구조를 유지하면서 **표현 모델만 한 단계 확장**하는 것이 목표다.

---

# 2. 데이터 모델

## 2.1 Canonical StoryPresentation

새 내부 표준 모델은 다음을 권장한다.

```ts
type StoryPresentation = {
  effects?: StoryEffectCue[];
  look?: StoryVisualLook;
  transition?: StoryTransition;
  actors?: StoryActorStaging;
  presetOrigin?: string;
};
```

`StoryLine`:

```ts
type StoryLine = {
  // 기존 필드
  presentation?: StoryPresentation;
};
```

최종 canonical v2 모델에서는 `effect`와 `presentation`을 동시에 수정 가능한 두 개의 source of truth로 두지 않는다.

구형 `effect`는 다음 경계에서만 취급한다.

```text
StoryDocument v1
기존 Excel / Google Sheet
구형 예시 데이터
        ↓
legacy adapter / migration
        ↓
StoryPresentation
```

즉 내부 편집·재생은 최종적으로 `presentation`만 본다.

---

## 2.2 Effects — 순간 효과

```ts
type StoryEffectCue = {
  id?: string;
  type: StoryEffectType;
  intensity?: "soft" | "normal" | "strong";
  trigger?: "scene-enter" | "with-dialogue" | "after-delay";
  delayMs?: number;
};
```

예:

```ts
effects: [
  { type: "flash", intensity: "normal", trigger: "scene-enter" },
  { type: "screen-crack", intensity: "strong", trigger: "scene-enter", delayMs: 80 },
  { type: "shake", intensity: "strong", trigger: "scene-enter", delayMs: 120 }
]
```

정책:

- 기본 UI는 최대 3개를 권장한다.
- runtime은 registry가 허용하는 범위에서만 실행한다.
- transform / opacity 중심으로 구현한다.
- 컷을 벗어나면 animation/timer를 반드시 cancel한다.
- 순간 효과에는 여러 컷 persistence를 넣지 않는다.

---

## 2.3 Look — 컷의 지속적 시각 상태

예:

```ts
look: {
  type: "flashback",
  intensity: "normal"
}
```

또는:

```ts
look: {
  type: "fractured-reality",
  intensity: "strong"
}
```

중요한 결정:

```text
StoryVisualLook 안에는
"3컷 동안", "해제할 때까지" 같은 runtime duration을 저장하지 않는다.
```

**각 컷은 그 컷에서 실제로 보여야 할 Look을 명시적으로 가진다.**

예를 들어 12~14컷이 균열된 현실이면:

```text
컷12 look=fractured-reality
컷13 look=fractured-reality
컷14 look=fractured-reality
컷15 look 없음
```

장점:

- 선택지 경로에 따라 상태가 새지 않는다.
- goto로 임의 컷에 이동해도 정확하다.
- 편집기에서 14컷만 바로 열어도 정확한 미리보기가 가능하다.
- 이어읽기 저장에 별도 Presentation state machine을 저장할 필요가 없다.
- 이전 컷 이동도 현재 history 구조를 그대로 사용한다.
- 장면 순서 변경이나 컷 삭제 시 숨겨진 inherited state를 추적할 필요가 없다.

---

## 2.4 사용자에게 보이는 "지속"은 범위 적용 명령으로 구현

사용자는 여전히 다음 UX를 사용할 수 있다.

```text
적용 범위
● 이 컷만
○ 다음 3컷
○ 범위 선택
```

그러나 이것은 runtime 데이터가 아니라 편집기 명령이다.

예:

```text
"회상 분위기를 다음 3컷에 적용"
       ↓ editor command
컷20 look=flashback
컷21 look=flashback
컷22 look=flashback
```

### 분기점 처리

`다음 N컷` 범위 안에 choice/goto 분기가 들어오면 조용히 배열 순서대로 적용하지 않는다.

편집기는 다음 중 하나로 전환한다.

```text
여기서 이야기가 갈라집니다.

[적용할 컷 직접 선택]
[현재 컷까지만 적용]
```

고급 버전에서만:

```text
[모든 갈래에 적용]
```

을 제공할 수 있다.

즉 "몇 컷 동안"이라는 쉬운 UX는 제공하되, 데이터는 항상 명시적이다.

---

## 2.5 Transition — 컷 진입 전환

초기 버전은 의미를 하나로 고정한다.

```text
transition = 이 컷이 표시되기 직전/진입 시 재생되는 전환
```

예:

```ts
transition: {
  type: "perspective-blackout",
  durationMs: 900,
  mode: "confirm",
  cue: "옹고집의 시점",
  title: "뒤바뀐 세상"
}
```

초기 지원:

- fade-black
- white-fade
- perspective-blackout

Transition이 active인 동안 필요한 경우:

- 다음/이전 키 입력 잠금
- 선택지 입력 잠금
- Enter/Space의 의미 변경
- transition 완료 후 본 장면 활성화

따라서 Transition 상태는 `StorySceneFrame`만으로 처리하지 않는다.

```text
StoryPlayer
   ↕
PresentationTransitionController
   ↕
StorySceneFrame / TransitionLayer
```

Editor preview는 같은 controller를 isolated preview mode로 사용한다.

---

## 2.6 Actor Staging — 기존 자동 레이아웃 위의 override

현재 놀스토리의 인물 크기/위치는 responsive 자동 계산이 이미 담당한다.

따라서 아래처럼 "절대 CSS 크기"를 저장하지 않는다.

금지 방향:

```ts
width: 487
left: 312
```

권장:

```ts
type StoryActorStaging = {
  left?: StoryActorOverride;
  right?: StoryActorOverride;
};

type StoryActorOverride = {
  xAnchor?: number;          // 0~100, stage 기준
  scaleMultiplier?: number; // 예: 0.8~1.25
  facing?: "left" | "right";
  opacity?: number;
  emphasis?: "normal" | "dim";
  spectral?: boolean;
};
```

레이아웃 적용 순서:

```text
원본 이미지 정보 / 투명 영역
        ↓
기존 자동 responsive layout
        ↓
StoryPresentation actor override
        ↓
사용자 화면 설정의 전체 보정
        ↓
최종 safe-area clamp / foot anchor 검증
```

`scaleMultiplier`는 기존 계산된 적정 크기에 곱한다.

이 구조는 인물마다 원본 이미지의 캔버스 여백이나 신체 비율이 달라서 발생하는 "한 명만 확대해 놓은 듯한" 문제를 줄이는 데 중요하다.

---

# 3. Registry / Preset 구조

## 3.1 Presentation Registry

기존 `STORY_EFFECTS`를 폐기하고 전혀 다른 체계를 만드는 대신 점진적으로 승격한다.

```ts
type PresentationRegistryEntry = {
  id: string;
  label: string;
  category: "effect" | "look" | "transition";
  defaultOptions: unknown;
  reducedMotionFallback?: unknown;
  conflictsWith?: string[];
};
```

구조상 효과/Look/Transition은 별도 registry 파일로 나눠도 되지만, editor가 검색할 수 있는 공통 catalog를 제공한다.

---

## 3.2 Quick Preset

초보자는 내부 Effects 배열을 직접 만들지 않는다.

예:

```text
💥 충격
🌫 회상
🌑 암전
⚡ 현실 균열
👁 시점 전환
```

`현실 균열` 프리셋 예:

```ts
{
  presetOrigin: "reality-crack",
  effects: [
    { type: "flash", ... },
    { type: "screen-crack", ... },
    { type: "shake", ... }
  ],
  look: { type: "fractured-reality", intensity: "normal" }
}
```

단, 프리셋 ID만 저장하지 않는다.

실제 expanded Presentation을 저장하고 `presetOrigin`은 UI provenance로만 둔다.

이렇게 해야 향후 프리셋 기본값이 바뀌어도 과거 작품이 변하지 않는다.

---

# 4. 편집 UI

## 4.1 기존 SceneFocusEditor 구조 유지

새로운 3열 전문 편집기로 바꾸지 않는다.

기존:

```text
[글] [왼쪽 이미지] [오른쪽 이미지] [배경]

기본 컷 설정
자산 선택
장면 미리보기
...
```

을 유지한다.

장면 미리보기 가까이에 한 줄만 추가한다.

```text
연출  [없음]                         [+ 연출 추가]
```

적용된 경우:

```text
연출  [⚡ 현실 균열 · 보통]          [변경]
```

Look이 여러 컷에 적용되어 있으면:

```text
연출  [⚡ 현실 균열 · 🌫 균열 분위기]
```

---

## 4.2 빠른 연출 패널

```text
빠른 연출
[충격] [회상] [암전] [현실 균열] [시점 전환]

강도
[약하게] [보통] [강하게]

시작
[컷 시작] [대사와 함께] [잠시 뒤]
```

### 지속 가능한 Look이 포함된 연출일 때만

다음 UI가 나타난다.

```text
분위기 적용 범위
[이 컷만 ▼]
```

옵션:

```text
이 컷만
다음 2컷
다음 3컷
범위 선택
```

순간 효과만 있는 `충격` 같은 프리셋에는 범위 UI를 보여주지 않는다.

이렇게 해야 "모든 효과에 지속성 설정이 붙어 있는" 불필요하게 복잡한 화면을 피할 수 있다.

---

## 4.3 고급 설정

기본적으로 닫혀 있다.

```text
[고급 설정 ▸]
```

열면:

```text
효과
1. Flash
2. Screen Crack
3. Shake
[+ 효과 추가]

장면 분위기
[균열된 현실 ▼]

전환
[없음 ▼]

인물 연출
왼쪽 인물 [설정]
오른쪽 인물 [설정]

타이밍
[세부 설정]
```

원칙:

- 내부 JSON이나 기술 용어를 기본 UI에 노출하지 않는다.
- ms 단위 delay, easing 등은 고급 설정에서만 노출한다.
- 숫자 입력보다 의미 기반 조절을 우선한다.

예:

```text
크기
[작게] [보통] [크게]
```

고급 사용자가 원할 때만 multiplier를 직접 조절한다.

---

## 4.4 Actor 직접 조작

가능하면 미리보기에서 인물을 클릭한 후:

- 좌우 drag → xAnchor
- 크기 handle 또는 slider → scaleMultiplier
- 좌우보기 버튼 → facing
- 환영 토글 → spectral

로 조절한다.

학생이 `x=27`, `scale=0.92`를 이해할 필요가 없게 한다.

---

## 4.5 컷 목록 / 네비게이션 표시

지속 Look은 각 컷에 명시되므로 컷 목록에서 쉽게 표시할 수 있다.

예:

```text
컷12  ⚡ 균열 + 🌫 균열 분위기
컷13           🌫 균열 분위기
컷14           🌫 균열 분위기
컷15  일반
```

연속된 동일 Look은 UI에서 범위처럼 묶어 보여줄 수 있지만, 저장 데이터는 각 컷에 명시적이다.

---

# 5. Renderer 구조

현재 `StorySceneFrame`을 중심으로 확장한다.

권장 개념 구조:

```text
StorySceneFrame
├─ VisualSurface
│   ├─ Background
│   ├─ Actors
│   └─ Look treatment
├─ EffectLayer
├─ DialogueLayer
└─ TransitionLayer
```

Look이 전체 장면 색조를 바꿔야 할 경우 Background/Actors를 묶는 VisualSurface에 filter 또는 overlay를 적용한다.

Dialogue까지 무조건 세피아/blur가 먹지 않도록 visual surface와 UI surface를 분리한다.

---

# 6. Reality Crack 구현

Pinky의 HTML/CSS 구조를 그대로 복사하지 않는다.

시각 문법만 일반화한다.

권장:

```text
RealityCrackEffect.tsx
```

구성:

- 하나의 SVG
- crack path
- glow path
- core path
- small gap/panel polygons
- 짧은 white flash
- 제한된 shake

Transient:

```text
screen-crack
```

Persistent Look:

```text
fractured-reality
```

둘을 분리한다.

저사양/Reduced Motion:

- panel displacement 제거
- shake 제거 또는 약화
- crack 이미지는 정적으로 유지
- Look의 의미 있는 상태는 유지

---

# 7. Transition과 플레이어 진행 제어

`perspective-blackout` 같은 연출은 다음 컷으로의 이동 자체와 연결되므로 단순 CSS가 아니다.

필요 상태:

```ts
type TransitionPlaybackState =
  | "idle"
  | "entering"
  | "waiting-confirm"
  | "finishing";
```

StoryPlayer는 blocking transition 동안:

- `goTo()` 중복 실행 방지
- Arrow/Space/Enter 기본 이동 방지
- choice 클릭 방지
- 저장/이동 메뉴가 transition 상태를 깨지 않도록 처리

`mode="auto"`:

```text
transition 재생 → 장면 표시
```

`mode="confirm"`:

```text
transition 재생 → 제목/설명 → [계속] → 장면 표시
```

이어읽기로 해당 컷에 진입하면 transition은 다시 재생해도 된다. 이는 "컷 진입 연출"이라는 의미와 일치한다.

---

# 8. StoryDocument v2 / `.knolstory`

## 8.1 내부 StoryDocument는 v2

```text
schemaVersion 1
→ legacy StoryLine.effect

schemaVersion 2
→ StoryLine.presentation
```

parser는 다음처럼 명시적으로 분기한다.

```text
parseStoryDocument
│
├─ v1 → parseV1 → migrateV1ToV2 → canonical v2
├─ v2 → parseV2 → canonical v2
└─ future version → unsupported error
```

단순히 `CURRENT_SCHEMA_VERSION = 2`로 바꾼 뒤 기존 검증기를 재사용해서는 안 된다.

---

## 8.2 v1 → v2 migration

```ts
line.effect
↓
line.presentation = {
  effects: [migrateLegacyEffect(line.effect)]
}
```

migration 후 canonical StoryLine에는 legacy `effect`를 남기지 않는다.

---

## 8.3 `.knolstory` outer container

현재 manifest / draft / playback / story / sharing 구조 자체가 바뀌지 않는다면:

```text
.knolstory container manifest v1 유지
```

내부에 들어가는 StoryDocument만 v2가 된다.

```text
.knolstory manifest v1
└─ StoryDocument v2
```

향후 asset container 구조 자체가 바뀔 때만 manifest version을 올린다.

---

## 8.4 `.nolstory`

읽기 호환 alias 정책을 유지한다.

구버전 `.nolstory` / `.knolstory`는 v1 migration을 거쳐 연출을 보존한다.

지원하지 않는 미래 버전은 조용히 열고 저장해서 정보를 제거하지 말고 명시적으로 거부한다.

---

# 9. Google Sheets / Excel

## 9.1 이번 버전은 기존 8탭 유지

8이라는 숫자를 아키텍처의 영구 제약으로 두지는 않는다.

그러나 현재 버전에서는 다음 이유로 8탭을 유지한다.

- 기존 공식 템플릿과 문서의 안정성
- 학생/교사의 익숙한 구조 유지
- Google Sheets와 Excel의 현재 공통 import 파이프라인 재사용
- Presentation 기능 때문에 스프레드시트 자체를 먼저 대규모 개편할 필요가 없음

9번째 탭은 **이번 개발 범위에서는 추가하지 않는다.**

향후 고급 연출을 스프레드시트에서 직접 대량 편집하려는 실제 요구가 생겼을 때 Spreadsheet Schema v3에서 별도 `연출` 탭을 검토한다.

---

## 9.2 `컷 대본` 열

현재 구형 연출 열은 import alias로 유지한다.

구형:

```text
연출 효과
연출 강도
연출 시점
연출 지연(초)
```

신규 공식 template에서는 사람에게 필요한 범위만 추가한다.

권장:

```text
연출 효과
연출 강도
연출 시점
연출 지연(초)
장면 분위기
전환
연출 데이터
```

`연출 데이터`는 고급 보존용이며 기본적으로 오른쪽 끝/숨김 열로 둘 수 있다.

Actor staging 수치, 복수 effects 개별 delay 등을 사람용 열로 수십 개 만들지 않는다.

---

## 9.3 Lossless Spreadsheet Envelope

단순히 full JSON과 visible 열을 둘 다 저장하면 어느 쪽이 최신인지 충돌할 수 있다.

따라서 `연출 데이터`는 다음 개념을 가진 envelope로 저장한다.

```ts
type SpreadsheetPresentationEnvelope = {
  version: 1;
  presentation: StoryPresentation;
  exportedProjection: {
    effect: string;
    intensity: string;
    trigger: string;
    delaySeconds: string;
    look: string;
    transition: string;
  };
};
```

Import:

1. `연출 데이터`가 있으면 canonical Presentation을 복구한다.
2. 현재 visible 열 값을 읽는다.
3. visible 열이 `exportedProjection`과 같으면 Presentation을 그대로 복구한다.
4. visible 열이 바뀌었다면 사용자가 수정한 필드만 Presentation에 반영한다.
5. visible 열에 없는 actor staging, 복합 효과 세부값 등은 보존한다.

이렇게 해야:

```text
웹에서 복잡한 연출 생성
→ Excel export
→ 대사 한 줄 수정
→ import
→ 연출 100% 보존
```

이 가능하다.

---

## 9.4 Google Sheets와 Excel은 같은 adapter

```text
StoryPresentation
      ↕
SpreadsheetPresentationAdapter
      ↕
공통 StoryImportSnapshot
      ↙               ↘
Google Sheets          Excel
```

Google Sheet용 독자 스키마와 Excel용 독자 스키마를 만들지 않는다.

---

## 9.5 공식 템플릿

수정 대상:

```text
scripts/generate-story-template.mjs
public/templates/놀퀴즈_스토리_템플릿.xlsx
```

가능한 곳에는 Excel data validation을 둔다.

예:

```text
연출 강도: 약하게 / 보통 / 강하게
연출 시점: 컷 시작 / 대사와 함께 / 잠시 뒤
장면 분위기: 없음 / 회상 / 균열된 현실 ...
전환: 없음 / 암전 / 시점 전환 ...
```

`연출 데이터`는 사용자가 직접 수정할 필요가 없음을 안내한다.

---

# 10. Publication / Shared / Remix

Presentation은 독자가 실제로 경험하는 작품 내용이다.

따라서 shared `.knolstory`에서 제거하면 안 된다.

Remix도 전체 Presentation을 복제한다.

Publication fingerprint는 기존 `effect` 대신 **정규화된 전체 Presentation**을 포함한다.

```text
effects
look
transition
actors
```

순서가 의미 없는 object key는 canonical normalization 후 hash한다.

Effects 배열은 순서가 실제 재생 순서이므로 배열 순서는 hash에 포함한다.

---

# 11. 컷 편집 명령과 Presentation

기존 컷 조작에도 정책이 필요하다.

## 복제

Presentation 전체를 복제한다.

## 이동

Presentation은 컷에 붙어 있으므로 함께 이동한다.

## 삭제

해당 컷 Presentation도 삭제된다. 다른 컷의 Look은 명시적이므로 연쇄 상태 정리가 필요 없다.

## 분할

기본 정책:

```text
첫 번째 분할 컷:
- transient effects 유지
- transition 유지
- look 유지
- actor staging 유지

두 번째 이후 분할 컷:
- transient effects 제거
- transition 제거
- look 유지
- actor staging 유지
```

이유:

- 한 번만 발생해야 할 순간 효과가 분할된 모든 대사마다 반복되는 것을 막음
- 장면 상태와 인물 배치는 같은 장면이므로 유지

## 병합

두 컷 중 하나라도 의미 있는 Presentation이 있으면 기본적으로 자동 병합을 금지한다.

향후 안전한 merge resolver를 만들기 전까지 보수적으로 간다.

---

# 12. Pinky Adapter

Pinky는 import source일 뿐 runtime dependency가 아니다.

```text
Pinky Beat
   ↓
adaptPinkyPresentation(beat)
   ↓
StoryPresentation
   ↓
일반 Story Maker project
```

매핑:

```text
visualMode=flashback
→ look=flashback

transition=reality-crack
→ effects: flash + screen-crack + shake

transition=reality-fractured
→ look=fractured-reality

perspective-blackout
→ transition=perspective-blackout

stageX
→ actors.*.xAnchor

scale
→ actors.*.scaleMultiplier

facing
→ actors.*.facing

spectral
→ actors.*.spectral
```

현재 `generate-story-examples.mjs`에서 presentation 정보를 버리는 로직을 제거하고 adapter를 호출한다.

완료 후 editor/player는 Pinky 출신인지 알 필요가 없어야 한다.

---

# 13. 접근성 / Reduced Motion

Reduced Motion에서 연출의 **의미**까지 지워서는 안 된다.

예:

```text
Shake
→ 제거 또는 매우 약하게

Flash
→ 밝기 변화 최소화

Reality Crack
→ 균열 SVG는 정적으로 유지

Fractured Reality Look
→ 유지

Perspective Blackout
→ 빠르고 부드러운 fade + 텍스트 유지
```

색상만으로 의미를 전달하지 않는다.

과도한 깜빡임 빈도 제한을 registry 규칙으로 둔다.

---

# 14. 성능 정책

초기 구현에서 GSAP, Canvas 게임 엔진, WebGL을 넣지 않는다.

기존 React + DOM/SVG + CSS + WAAPI를 유지한다.

규칙:

- active transient effects 권장 최대 3개
- 현재 컷 Presentation만 mount
- animation 종료 후 `will-change` 제거
- scene change / unmount 시 timer + WAAPI cancel
- full-screen blur/filter 남용 금지
- Reality Crack은 많은 DOM 파편 대신 하나의 SVG 중심
- 다음 컷 asset preload는 기존 player 흐름 확장
- transition 중 중복 navigation 금지

성능 판단 기준은 전체 작품의 컷 수가 아니라 **현재 active scene의 DOM/animation cost**로 둔다.

---

# 15. 구현 파일 구조

새 파일 권장:

```text
app/presentation/
  story-presentation.ts
  story-presentation-validation.ts
  story-presentation-registry.ts
  story-presentation-presets.ts
  story-presentation-spreadsheet.ts

  effects/
    ShakeEffect.tsx
    FlashEffect.tsx
    RealityCrackEffect.tsx

  looks/
    FlashbackLook.tsx
    FracturedRealityLook.tsx

  transitions/
    FadeTransition.tsx
    PerspectiveBlackoutTransition.tsx

  actors/
    actor-staging.ts

  runtime/
    PresentationController.ts
    TransitionController.ts
```

기존 파일 중 주요 수정 대상:

```text
app/story-data.ts
app/story-scene-effect.ts
app/hooks/useSceneEffect.ts
app/components/StoryStage.tsx
app/components/SceneEffectEditor.tsx
app/components/StoryPlayer 관련 파일
app/story-project-document.ts
app/story-project-validation.ts
app/story-sheet.ts
app/story-workbook.ts
app/story-import.ts
app/story-file.ts
app/story-publication.ts
app/story-commands.ts
scripts/generate-story-examples.mjs
scripts/generate-story-template.mjs
```

실제 구현 시 현재 repository 구조를 다시 확인해 최종 위치를 결정한다. 무리하게 새 디렉터리를 만드는 것보다 기존 naming convention을 우선한다.

---

# 16. 단계별 개발 순서

## Phase 0 — 기준선 고정

코드를 바꾸기 전에 regression fixture를 만든다.

대표 장면:

1. 기존 shake
2. 기존 flash-red
3. 기존 fade-black
4. 기존 crack
5. 기존 spotlight
6. 일반 대사 → effect → 일반 대사
7. 선택지 분기
8. goto
9. 이어읽기
10. 기존 Excel/Google Sheet effect import
11. 기존 `.knolstory` v1
12. Pinky reality-crack
13. Pinky reality-fractured
14. Pinky flashback
15. Pinky perspective-blackout
16. stageX/scale
17. spectral

Phase 0에서 스크린샷/브라우저 fixture를 확보한다.

---

## Phase 1 — Presentation 타입 + registry

- `StoryPresentation` 타입 작성
- effects/look/transition/actors validator 작성
- registry 작성
- preset compiler 작성
- normalize/canonicalize 함수 작성

아직 기존 UI를 바꾸지 않는다.

---

## Phase 2 — StoryDocument v2 migration

- `StoryDocumentEnvelopeV1` 정의
- `StoryDocumentEnvelopeV2` 정의
- parseV1 / parseV2 분리
- `migrateV1ToV2()` 작성
- legacy `effect` → Presentation 변환
- 미래 버전 거부 테스트
- `.knolstory` v1 document import 테스트

이 단계 완료 후 canonical runtime project는 v2 Presentation을 사용한다.

---

## Phase 3 — 기존 효과를 Presentation runtime으로 이관

- `StorySceneFrame`이 `presentation`을 받게 변경
- 기존 5개 effect를 effects[] renderer로 옮김
- `useSceneEffect` cleanup 로직 재사용/승격
- editor preview와 player renderer 일치 확인

이 시점에서 기능상 사용자 체감은 거의 없어야 한다.

목표는 **기존 기능 100% 보존 상태의 내부 구조 교체**다.

---

## Phase 4 — 파일/Spreadsheet round-trip

UI 확장보다 먼저 데이터 손실 경로를 막는다.

- `.knolstory` v2 저장/읽기
- shared/remix
- publication fingerprint
- Excel export/import
- Google Sheets import
- SpreadsheetPresentationEnvelope
- 구형 4개 연출 열 migration
- 공식 template 업데이트

완료 조건:

```text
웹 → Excel → 웹
웹 → .knolstory → 웹
웹 → shared/remix
```

에서 Presentation이 손실되지 않음.

---

## Phase 5 — 기본 연출 UI

기존 편집화면에:

```text
연출 [없음] [+ 추가]
```

추가.

Quick Preset:

- 충격
- 회상
- 암전
- 현실 균열
- 시점 전환

간단 설정:

- 강도
- 시작
- 필요할 때만 Look 적용 범위

기존 컷 편집 속도를 저하시키지 않는지 확인한다.

---

## Phase 6 — Look + 범위 적용 command

- flashback look
- fractured-reality look
- selected-range apply
- next-N apply
- branch detection
- branch가 나오면 직접 컷 선택 UI
- cut list badge

중요:

runtime persistence state machine은 만들지 않는다.

---

## Phase 7 — Transition

- fade
- perspective-blackout
- TransitionController
- StoryPlayer navigation lock
- keyboard/choice interaction
- editor preview
- reduced motion

---

## Phase 8 — Actor Staging

- xAnchor
- scaleMultiplier
- facing
- opacity/emphasis
- spectral
- 기존 responsive layout과 결합
- direct manipulation
- safe-area clamp

인물 비율 regression test를 반드시 포함한다.

---

## Phase 9 — Reality Crack 고도화

- SVG crack renderer
- flash/crack/shake preset
- fractured-reality Look
- reduced motion fallback
- 모바일/저사양 성능 검증

---

## Phase 10 — Pinky Adapter + 옹고집전 복원

- `adaptPinkyPresentation()`
- `generate-story-examples.mjs` 변경
- Pinky presentation fields 보존
- 옹고집전 대표 컷 시각 비교
- 편집기에서 일반 Presentation으로 수정 가능 확인

이 단계에서만 Pinky 원작 parity 작업을 본격 진행한다.

---

## Phase 11 — 안정화

- browser regression
- mobile/tablet
- reduced motion
- file compatibility
- sheet compatibility
- branching
- save/resume
- undo/redo
- cut split/merge/duplicate
- publication fingerprint
- performance

---

# 17. 테스트 매트릭스

## 데이터

- v1 effect → v2 effects[0]
- malformed presentation reject
- unknown future schema reject
- preset expanded data preservation

## Runtime

- multiple effects order
- effect cancellation on cut change
- look renders only where explicitly stored
- branch A look does not leak into branch B
- direct jump to cut renders correct Look
- previous navigation renders correct Look
- resume renders correct Look

## Transition

- keyboard blocked while required
- double navigation impossible
- choice cannot be accidentally skipped
- confirm transition works
- reduced motion works

## Actor

- small/large source images
- transparent padding
- group image
- portrait/full-body
- mobile safe-area
- scaleMultiplier does not produce giant-like mismatch

## Spreadsheet

- legacy Excel import
- legacy Google Sheet import
- new Excel round-trip
- full advanced presentation round-trip
- visible column one-field edit preserves hidden advanced data

## File

- old `.knolstory`
- old `.nolstory`
- v2 `.knolstory`
- shared
- remix
- unsupported future version

## Editing commands

- duplicate
- move
- delete
- split
- merge restriction
- undo/redo

---

# 18. 완료 기준

## A. 기본 사용자

- 연출이 없는 컷은 기존 UI와 거의 동일하다.
- 흔한 연출은 1~2번 선택으로 적용 가능하다.
- 보통 사용자는 강도/시작 정도만 조절하면 된다.
- 지속되는 분위기는 "적용 범위"로 쉽게 설정한다.
- 데이터 모델의 Effect/Look/Transition 구분을 몰라도 된다.

## B. 고급 사용자

- 복수 effects를 조합할 수 있다.
- Look을 별도로 설정할 수 있다.
- Transition을 설정할 수 있다.
- Actor staging을 조절할 수 있다.
- Pinky 수준의 대표 연출을 일반 UI에서 수정할 수 있다.

## C. Runtime

- 편집 preview와 player가 같은 renderer를 사용한다.
- 분기 경로에 Presentation 상태가 새지 않는다.
- transition이 navigation과 충돌하지 않는다.
- reduced motion이 서사 정보를 제거하지 않는다.

## D. 데이터 호환

- 기존 Google Sheet를 읽을 수 있다.
- 기존 Excel을 읽을 수 있다.
- 기존 `.knolstory` / `.nolstory`를 읽을 수 있다.
- 새 고급 연출을 Excel/Google Sheet 경유 후에도 보존한다.
- shared/remix에 Presentation이 유지된다.
- fingerprint가 Presentation 변화를 반영한다.

## E. Pinky / 옹고집전

최소 다음을 일반 Presentation으로 재현한다.

- flashback
- reality crack
- fractured reality
- perspective blackout
- actor stage X
- scale
- facing
- spectral

그리고 해당 컷을 놀스토리 편집기에서 열었을 때 Pinky 전용 데이터가 아니라 일반 연출 UI로 편집할 수 있어야 한다.

---

# 19. 이번 버전에서 하지 않을 것

다음은 의도적으로 제외한다.

- Premiere/After Effects식 전체 timeline
- 프레임 단위 keyframe 편집
- 자유로운 수십 개 effect 동시 실행
- WebGL/3D scene engine
- Pinky 전용 runtime branch
- StoryLine마다 숨겨진 runtime persistent-state chain
- 모든 actor 값을 숫자로 노출하는 전문 툴 UI
- 8탭을 지키기 위해 수십 개 spreadsheet 열을 추가하는 것
- 반대로 단순히 고급 연출 때문에 당장 9번째 탭을 추가하는 것

---

# 20. 향후 9번째 `연출` Sheet 탭을 검토할 조건

8탭은 영구 계약이 아니다.

다음 요구가 실제로 커질 경우 Spreadsheet Schema 차기 버전에서 `연출` 탭을 추가한다.

- Excel/Google Sheets 자체에서 복수 effect를 직접 대량 편집해야 함
- 한 컷당 actor staging을 자주 직접 수정해야 함
- transition 메타데이터가 크게 늘어남
- 숨김 JSON 열만으로는 관리/디버깅이 어려워짐
- 교사가 웹 편집기 대신 Sheet에서 연출을 집중 관리하려는 실제 사용 패턴이 확인됨

그때는:

```text
기존 8탭 파일 → 계속 import 가능
새 9탭 파일 → Spreadsheet Schema 차기 버전
```

으로 진화한다.

즉 **현재 호환성은 지키되 미래 구조를 막지는 않는다.**

---

# 21. 최종 아키텍처

```text
                    ┌─────────────────────────┐
                    │   StoryPresentation     │
                    │ effects / look          │
                    │ transition / actors     │
                    └───────────┬─────────────┘
                                │
          ┌─────────────────────┼─────────────────────┐
          │                     │                     │
          ▼                     ▼                     ▼
     Web Editor            Story Player        Spreadsheet Adapter
          │                     │                     │
          │                     │              ┌──────┴──────┐
          │                     │              ▼             ▼
          │                     │         Google Sheets     Excel
          │                     │
          └──────────┬──────────┘
                     ▼
               StorySceneFrame
                     │
                     ▼
         shared renderer / preview

StoryDocument v1 ──migration──▶ StoryDocument v2
                                   │
                                   ├── .knolstory
                                   ├── Shared
                                   ├── Remix
                                   └── Publication fingerprint

Pinky Beat ──PinkyPresentationAdapter──▶ StoryPresentation
```

---

# 22. 구현 원칙 한 문장

> **놀스토리의 현재 컷 중심 구조와 쉬운 편집 경험은 유지하고, 복잡성은 StoryPresentation과 adapter 계층 안으로 밀어 넣으며, 사용자가 보는 연출 UI는 필요할 때만 단계적으로 확장한다.**

이 원칙을 벗어나면 구현 중 다시 설계를 검토한다.
