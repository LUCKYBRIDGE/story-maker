# 스토리메이커 통합 개발 기획안
## 현재 놀스토리 구조 보존 + 숏스토리 추가 + 간소화된 검증 기준

- 대상 저장소: `LUCKYBRIDGE/story-maker`
- 기준 브랜치: `main`
- 기준 확인일: 2026-09-17
- 확인한 기준 `main`: `189240b8e1751d406e15767aecef14d4dca96839`
- 문서 목적: 이미 구현된 놀스토리의 구조와 데이터를 안정적으로 유지하면서, 별도 형식인 「숏스토리」와 읽기 전용 HTML 공유를 점진적으로 추가한다.
- 개발 원칙: **기존 기능을 지키되 검증 자체가 개발의 대부분을 차지하지 않도록 한다.**

---

# 1. 전체 방향

스토리메이커는 앞으로 두 가지 창작 형식을 제공한다.

```text
                           스토리메이커
                                │
                 ┌──────────────┴──────────────┐
                 │                             │
              놀스토리                       숏스토리
          기존 주력 스토리게임              새 그림책형 창작
                 │                             │
            StoryProject                ShortStoryProject
                 │                             │
            StoryStudio                ShortStoryWorkspace
                 │                       ├─ Editor
       StoryBookPlayback/Player          └─ Reader
                 │                             │
            .knolstory                   .shortstory
           편집 작업 파일                 편집 작업 파일
                 │                             │
                 └──────────┬──────────────────┘
                            │
                      읽기용으로 내보내기
                            ↓
                         .html
                  친구에게 보내는 감상본
```

핵심은 기존 놀스토리를 범용 모델로 뜯어고치는 것이 아니다.

> **놀스토리는 현재 구조를 유지하고, 숏스토리는 옆에 독립된 형식으로 추가한다.**

---

# 2. 현재 놀스토리에서 이미 구현된 것

현재 놀스토리는 초기 MVP 수준을 넘어 다음 흐름이 실제로 구현되어 있다.

```text
메인
├─ 이야기 읽기
├─ 이야기 변경 → 서재
├─ 서재 입장 → 서재
└─ 나만의 이야기 → 창작 관리
```

서재:

```text
기본 이야기
내 작품
모두의 이야기
새 이야기 만들기
```

편집:

```text
이야기 구성
   ↓
대본 전체 ↔ 컷 꾸미기
   ↓
플레이에 적용
   ↓
표지 → 플레이 → 선택·분기 → 뒤표지
```

현재 기본 작품:

```text
별주부전
옹고집전
선녀와 나무꾼
```

현재 원작 읽기:

```text
별주부전
옹고집전
선녀와 나무꾼
```

도 별도로 존재한다.

---

# 3. 현재 놀스토리에서 반드시 보존할 계약

## 3.1 StoryProject

현재 놀스토리 작품 모델이다.

대표적으로 다음을 포함한다.

```text
StoryProject
├─ source
├─ cover
├─ planning
├─ creativeMemos
├─ speakerNames
├─ chapters[]
└─ lines[]
```

`StoryLine`에는 대사·해설·이미지·연출·선택·분기 정보가 들어간다.

이 구조는 **놀스토리 전용 모델로 유지한다.**

---

## 3.2 draft / playback

현재 놀스토리에서는:

```text
draft
≠
playback
```

이다.

- draft: 편집 중 작품
- playback: 플레이에 적용한 읽기 버전

이 구조는 그대로 유지한다.

---

## 3.3 기존 로컬 작품 보관

현재:

```text
storygame:projects:v1
```

에 놀스토리 작품을 보관하며 최대 편집 작품 수는 2개다.

숏스토리를 추가한다고 이 데이터를 즉시 통합 저장소로 마이그레이션하지 않는다.

---

## 3.4 `.knolstory`

현재 공식 놀스토리 파일 규격이다.

역할:

```text
작품 백업
다른 기기로 이동
이어 편집
```

기존 `.nolstory` 호환도 유지한다.

---

## 3.5 기존 Excel

현재 놀스토리는 8개 탭 Excel 구조를 사용한다.

```text
시작하기
이야기 구성
장의 흐름
장의 자료
화자
컷 대본
창작 메모
리소스
```

숏스토리 때문에 이 규격을 바꾸지 않는다.

---

# 4. 숏스토리를 StoryProject의 모드로 넣지 않는다

다음처럼 만들지 않는다.

```ts
storyMode: "standard" | "short"
```

현재 `StoryProject`가 이미:

- 장
- 컷
- 이야기 구성
- 화자
- flow
- 플레이 적용
- 창작 메모

와 깊게 결합되어 있기 때문이다.

숏스토리를 이 구조 안에 억지로 넣으면:

```text
불필요한 필드 증가
조건문 증가
Editor 복잡화
파일 규격 혼선
Excel 혼선
```

이 생긴다.

따라서:

```text
StoryProject
ShortStoryProject
```

를 분리한다.

---

# 5. 놀스토리와 숏스토리의 역할

## 놀스토리

스토리게임 형식이다.

```text
컷
→ 대사·해설
→ 캐릭터·배경 연출
→ 선택
→ 분기
→ 합류
→ 엔딩
```

중요한 것:

- 한 컷의 몰입
- 선택
- 실제 선택 경로
- 분기
- 복수 엔딩
- 시네마틱 연출

---

## 숏스토리

짧은 그림책·장면책 형식이다.

```text
여러 장면 확인
→ 그림과 글 읽기
→ 글 수정
→ 그림 변경
→ 장면 추가·삭제
→ 순서 변경
→ 짧은 책 완성
```

중요한 것:

- 전체 이야기 흐름
- 줄거리 요약
- 장면 배열
- 직접 쓰기
- 책으로 완성하기

---

# 6. 공통으로 사용할 것과 분리할 것

## 공통 사용

- 서재
- 기본 작품 목록
- Asset ID
- 공통 이미지
- 이미지 URL resolver
- 디자인 토큰
- 글꼴
- 버튼 스타일
- 다운로드 유틸
- 표지의 시각 언어

## 분리

- 작품 모델
- Editor
- Reader
- Excel 규격
- 작업 파일 규격

즉:

> **같은 서비스 안의 두 도구이지 하나의 Editor에 두 모드를 넣는 구조가 아니다.**

---

# 7. 파일 역할을 명확히 나눈다

| 파일 | 역할 | 계속 편집 |
|---|---|---:|
| `.knolstory` | 놀스토리 작업 파일 | O |
| `.shortstory` | 숏스토리 작업 파일 | O |
| `.html` | 친구에게 보내는 읽기 전용 공유본 | X |
| PDF | 인쇄 결과물 | X |

---

# 8. `.knolstory`

사용자에게는:

```text
놀스토리 작업 파일로 저장
나중에 다시 열어 계속 만들 수 있어요.
```

라는 의미다.

흐름:

```text
작품 만들기
→ .knolstory
→ 다른 기기
→ 스토리메이커에서 열기
→ 이어서 편집
```

---

# 9. `.shortstory`

사용자에게는:

```text
숏스토리 작업 파일로 저장
나중에 다시 열어 계속 만들 수 있어요.
```

이다.

흐름:

```text
숏스토리 만들기
→ .shortstory
→ 다른 기기
→ 스토리메이커에서 열기
→ 이어서 편집
```

---

# 10. `.html`

HTML은 작업 파일이 아니다.

```text
친구에게 공유
→ 작품.html
```

받은 사람:

```text
작품.html 열기
→ 브라우저에서 바로 감상
```

받은 사람은:

- 스토리메이커 홈페이지 접속
- 파일 업로드
- 로그인
- 회원가입

을 할 필요가 없어야 한다.

---

# 11. HTML은 다시 편집 파일로 되돌리지 않는다

초기에는:

```text
.html → .knolstory
.html → .shortstory
```

변환을 제공하지 않는다.

원본을 계속 수정하려면:

```text
.knolstory
.shortstory
```

를 보관해야 한다.

이 구분은 제품 전반에서 일관되게 유지한다.

---

# 12. 현재 `.knolstory shared`는 하위 호환으로 남긴다

현재 코드에는 `.knolstory`의 공유 파일 형식도 있다.

이를 바로 삭제하지 않는다.

기존:

```text
.knolstory shared
→ 읽기
→ 허용된 경우 고쳐쓰기
```

는 계속 열 수 있게 한다.

다만 새 UI에서는 앞으로:

```text
작업 파일로 저장 → .knolstory
친구에게 공유 → .html
```

을 기본 흐름으로 한다.

---

# 13. 현재 `모두의 이야기`도 당장 없애지 않는다

현재 `모두의 이야기`는 불러온 공유 `.knolstory`를 읽는 구조와 연결되어 있다.

HTML 공유는 파일 자체에서 감상하므로 서재에 넣을 필요가 없다.

따라서:

- 기존 모두의 이야기 유지
- HTML 공유 추가
- 온라인 공개 커뮤니티는 별도 과제

로 둔다.

---

# 14. 숏스토리 데이터 구조

예:

```ts
type ShortStoryProject = {
  id: string;
  title: string;
  description: string;
  authorDisplayName?: string;
  source?: ShortStorySource;
  cover?: ShortStoryCover;
  pages: ShortStoryPage[];
  updatedAt: string;
};
```

```ts
type ShortStoryPage = {
  id: string;
  order: number;
  title?: string;
  text: string;
  backgroundId?: string;
  leftAssetId?: string;
  rightAssetId?: string;
};
```

초기에는 필요한 데이터만 둔다.

---

# 15. 숏스토리에 넣지 않을 것

초기에는 다음 개념을 넣지 않는다.

- 장
- 3·4·5단계 이야기 구성
- 선택지
- 분기
- 합류
- 복수 엔딩
- StoryFlow
- 플레이 적용
- 장별 자원
- 복잡한 창작 메모

필요해질 때 숏스토리에 맞는 방식으로 별도 추가한다.

---

# 16. ShortStoryReader

별도 Reader를 만든다.

```text
ShortStoryReader
```

두 가지 보기를 기본으로 한다.

## 책으로 읽기

```text
표지
→ 1쪽
→ 2쪽
→ ...
→ 뒤표지
```

## 한눈에 보기

```text
[1] [2] [3]
[4] [5] [6]
```

목적:

- 줄거리 파악
- 요약
- 장면 순서 이해
- 전체 작품 확인

---

# 17. ShortStoryEditor

기존 `StoryStudio`를 축소해서 사용하지 않는다.

```text
ShortStoryWorkspace
└─ ShortStoryEditor
```

기본 화면:

```text
[ 그림 ][ 그림 ][ 그림 ]
[ 글   ][ 글   ][ 글   ]
  1쪽     2쪽     3쪽

       [+ 장면 추가]
```

기본 기능:

- 글 수정
- 그림 바꾸기
- 장면 추가
- 장면 삭제
- 장면 복제
- 순서 이동

---

# 18. StoryStudio는 가능한 한 그대로 둔다

현재 `StoryStudio.tsx`는 이미 많은 책임을 갖고 있다.

숏스토리 로직을 여기에 직접 넣지 않는다.

허용:

```text
숏스토리 화면으로 들어가기
숏스토리에서 서재로 돌아오기
```

정도의 얇은 연결.

실제 숏스토리 상태·명령·저장은 별도 모듈에서 처리한다.

---

# 19. 숏스토리 저장소는 별도로 시작한다

기존:

```text
storygame:projects:v1
```

은 놀스토리 전용으로 유지한다.

숏스토리:

```text
storygame:shortstories:v1
```

을 새로 만든다.

초기에는 두 저장소를 억지로 합치지 않는다.

---

# 20. 서재에서만 통합해 보여준다

사용자는 저장 방식까지 알 필요가 없다.

```text
놀스토리 저장소 ─┐
                  ├→ 서재
숏스토리 저장소 ─┘
```

서재용 표현 모델을 둔다.

예:

```ts
type LibraryWork =
  | NolStoryLibraryWork
  | ShortStoryLibraryWork;
```

카드에서는 작은 표시만 한다.

```text
놀스토리
숏스토리
```

---

# 21. 기존 2개 작품 제한은 당장 건드리지 않는다

숏스토리 개발과 동시에 기존 제한 정책까지 재설계하지 않는다.

초기에는 기존 놀스토리 보관함은 그대로 둔다.

숏스토리 보관 한도는 별도로 단순하게 정한 뒤, 실제 사용량을 보고 통합 정책을 나중에 결정한다.

---

# 22. 서재 기본 작품 액션을 먼저 정상화한다

현재 기본 작품에 직접 보이는 것은:

```text
놀스토리 읽기
모두의 이야기 읽기
편집하기
```

이고 `원작 읽기`는 별도 DOM 삽입 방식이다.

숏스토리 버튼을 같은 방식으로 추가하지 않는다.

목표:

```text
[ 원작 읽기 ]
[ 놀스토리 읽기 ]
[ 숏스토리 ]
[ 모두의 이야기 읽기 ]
[ 편집하기 ]
```

모두 `StoryDiscovery`에서 정상적으로 렌더링한다.

---

# 23. Base Story Capability

예:

```ts
type BaseStoryCapabilities = {
  classicReading?: string;
  nolstoryReading: boolean;
  shortStoryPresetId?: string;
  sharedStories: boolean;
  editableBase: boolean;
};
```

이 데이터에 따라 버튼을 보여준다.

현재 `MutationObserver` 방식은 이후 제거한다.

---

# 24. 원작 읽기는 숏스토리로 바꾸지 않는다

현재 원작 읽기에는 이미:

- 긴 서사
- 컷 단위 진행
- 이미지 연출
- 책갈피/이어읽기

가 구현되어 있다.

따라서:

```text
원작 읽기
놀스토리
숏스토리
```

세 형식을 분리한다.

---

# 25. 기본 작품에서 숏스토리 진입

예:

```text
별주부전
├─ 원작 읽기
├─ 놀스토리 읽기
├─ 숏스토리
├─ 모두의 이야기 읽기
└─ 편집하기
```

숏스토리:

```text
[ 책으로 읽기 ]
[ 한눈에 보기 ]
[ 직접 만들어 보기 ]
[ 활동지 인쇄 ]
```

---

# 26. 기본 숏스토리 장면 선정

원작 읽기의 많은 컷을 기계적으로 일정 간격으로 줄이지 않는다.

각 이야기의 핵심 사건을 사람이 선정한다.

예:

```text
시작
중요한 만남
갈등
변화
위기
해결
결말
```

---

# 27. 숏스토리 기본 쪽수

고정된 규격을 강제하지 않는다.

```text
5쪽
7쪽
8쪽
9쪽
11쪽
```

도 가능하다.

작품에 필요한 만큼 기본 프리셋을 만들고, 사용자는 이후 자유롭게 추가·삭제한다.

---

# 28. 빈 숏스토리

현재 `새 이야기 만들기`는 그대로 둔다.

옆에 별도:

```text
숏스토리 만들기
```

를 추가한다.

진입:

```text
6쪽
8쪽
10쪽
직접 정하기
```

정도만 선택하고 바로 Editor로 간다.

---

# 29. 공통 이미지 자산은 재사용한다

다음을 그대로 활용한다.

```text
STORY_ASSETS
Asset ID
resolveAssetUrl
현재 taxonomy
```

숏스토리용 이미지를 별도 저장 시스템으로 만들 필요는 없다.

taxonomy v2 완성도 숏스토리의 선행 조건으로 두지 않는다.

---

# 30. `.shortstory` 규격

예:

```json
{
  "manifest": {
    "format": "shortstory",
    "version": 1,
    "kind": "project",
    "exportedAt": "...",
    "appVersion": "..."
  },
  "assets": [],
  "project": {}
}
```

내부는 JSON 기반으로 단순하게 유지한다.

---

# 31. `.shortstory` 파일 처리

별도 모듈:

```text
shortstory-file.ts
```

함수 예:

```ts
parseShortStoryFile()
encodeShortStoryFile()
readShortStoryFile()
downloadShortStoryFile()
```

---

# 32. 파일 검사는 필요한 만큼만 한다

과도한 검증 로직을 만들지 않는다.

필수 항목:

- format/version 확인
- project와 pages 기본 타입 확인
- page id 중복 방지
- 알려진 내장 asset ID인지 확인
- 지나치게 큰 파일 차단

그 외 세부 콘텐츠 품질은 Editor가 정상적으로 다룰 수 있으면 파일 parser에서 과도하게 검사하지 않는다.

---

# 33. 숏스토리 Excel

놀스토리 8탭 Excel과 별도로 만든다.

권장:

```text
시작하기
이야기 정보
장면
리소스
```

실제 핵심 데이터는:

```text
이야기 정보
장면
```

이다.

---

# 34. 장면 시트

예:

| 순서 | 장면 제목 | 글 | 배경 | 왼쪽 인물 | 오른쪽 인물 |
|---:|---|---|---|---|---|
| 1 | 용궁의 근심 | 용왕이 병에 걸렸어요. | 용궁 | 용왕 | 별주부 |
| 2 | 육지로 | 별주부가 토끼를 찾으러 떠났어요. | 바닷가 | 별주부 | |
| 3 | 만남 | ... | 들판 | 토끼 | 별주부 |

---

# 35. 숏스토리 Excel 생성 명령

현재 놀스토리:

```text
npm run template:generate
```

는 유지한다.

새 명령:

```text
npm run template:shortstory
```

정도로 분리한다.

---

# 36. Google 시트

숏스토리 Excel과 같은 열 구조를 사용한다.

```text
공개 Google Sheet
→ 한 번 읽기
→ ShortStoryProject
```

만 지원한다.

실시간 동기화나 Google 계정 연동은 하지 않는다.

---

# 37. CreationHub는 천천히 확장한다

현재 CreationHub를 완전히 generic하게 다시 만들지 않는다.

먼저:

```text
작업 파일 열기
```

에서:

```text
.knolstory
.nolstory
.shortstory
```

를 식별하도록 한다.

Excel/Google 시트 UI는 필요할 때 놀스토리/숏스토리 선택을 붙인다.

---

# 38. 공유 HTML의 제품 계약

공유 경험은 단순해야 한다.

```text
작품 완성
→ 친구에게 공유
→ 작품.html
→ 친구에게 전달
→ 친구가 파일 열기
→ 바로 읽기
```

친구는 놀스토리 사이트의 메뉴를 거칠 필요가 없다.

---

# 39. HTML에 들어가는 것

```text
작품 제목
작품 종류
읽기용 작품 데이터
사용 asset ID
runtime version
최소 bootstrap
```

---

# 40. 인터넷으로 가져오는 것

현재 배포된 스토리메이커에서:

```text
Share Runtime
CSS
공통 이미지
글꼴
필요한 공통 효과
```

를 가져온다.

학생 작품 HTML 자체를 서버에 올리는 것은 아니다.

---

# 41. 작품 데이터는 서버에 저장하지 않는다

HTML을 연 사람이 작품을 읽을 때:

```text
작품 데이터
→ 브라우저 안에서 Reader에 전달
```

한다.

작품 전체를 중앙 작품 서버에 저장하지 않는다.

---

# 42. 공유 HTML은 GitHub에 올리지 않는다

학생의:

```text
민수의별주부전.html
지우의옹고집전.html
```

은 개인끼리 주고받는 파일이다.

GitHub나 중앙 DB에 업로드할 필요가 없다.

---

# 43. Runtime은 안정된 운영 주소 사용

공유 HTML은 임시 개발 주소를 참조하지 않는다.

운영 배포 주소의 고정 경로를 사용한다.

예:

```text
/share-runtime/v1/
```

---

# 44. Runtime 버전

HTML 안에:

```text
runtimeVersion: 1
```

정도를 기록한다.

v2가 생겨도 기존 v1 HTML이 갑자기 다른 방식으로 렌더링되지 않도록 한다.

다만 초기부터 복잡한 Asset Catalog 버전 체계를 과도하게 만들지는 않는다.

기존 asset ID를 최대한 유지하는 것으로 먼저 충분하다.

---

# 45. 공유 HTML 구현 방식

우선 후보:

```text
로컬 HTML
→ iframe으로 원격 Runtime
→ postMessage로 작품 데이터 전달
```

이 방식이 실제 주요 환경에서 문제없이 작동하면 사용한다.

문제가 있으면:

```text
HTML에 작은 Reader 코드 포함
+ 이미지만 온라인 로드
```

방식으로 바꾼다.

---

# 46. HTML 기술 확인은 작게 한다

HTML 공유를 만들 때 처음부터 여러 운영체제·브라우저를 전수 검사하지 않는다.

초기 구현 확인:

```text
Windows 또는 Mac의 주 브라우저 1개
+
사용자가 실제 많이 쓰는 태블릿 1종
```

정도로 한다.

기능이 실제 공유 단계까지 완성된 뒤 필요하면 다른 환경을 추가 확인한다.

---

# 47. 놀스토리 공유 HTML

놀스토리는:

```text
draft
→ 플레이에 적용
→ playback
→ HTML 공유
```

흐름으로 한다.

HTML에는 `playback`의 읽기 정보만 넣는다.

편집용:

- planning
- creativeMemos
- sheetUrl
- 각종 편집 메모

는 넣지 않는다.

---

# 48. 기존 StoryPlayer를 공유 HTML에 그대로 넣지 않는다

현재 StoryPlayer는:

- 북마크
- 로컬 저장
- 편집 복귀
- 앱 전용 표시 설정

등을 포함한다.

공유 HTML에서는 필요한 재생 부분만 사용한다.

공통 부분이 실제로 필요할 때 작은 `PlaybackCore`를 추출한다.

이를 이유로 StoryPlayer 전체를 미리 대규모 리팩터링하지 않는다.

---

# 49. 숏스토리 공유 HTML

숏스토리는 간단하다.

```text
cover
pages
asset IDs
```

를 사용하여:

```text
표지
→ 책 읽기
→ 한눈에 보기
→ 뒤표지
```

만 제공한다.

편집 기능은 없다.

---

# 50. 네트워크 실패

온라인 연동 HTML이므로 인터넷이 없으면 공통 자원을 불러올 수 없다.

빈 화면 대신 최소 안내를 보여준다.

```text
이 작품을 읽으려면 인터넷 연결이 필요해요.
```

복잡한 오프라인 대응은 초기 범위에서 제외한다.

---

# 51. 숏스토리 활동지

`ShortStoryProject`에서 바로 출력한다.

```text
[ 그림 ]

________________
________________
________________
```

별도의 활동지 데이터 파일을 만들지 않는다.

---

# 52. 숏스토리 책 인쇄

같은 데이터에서:

```text
앞표지
본문
필요한 빈 면
뒤표지
```

를 출력한다.

---

# 53. 페이지 수 제한 없음

사용자는:

```text
5
6
7
8
9
10
11
...
```

쪽을 자유롭게 사용할 수 있다.

4의 배수를 강제하지 않는다.

---

# 54. 빈 인쇄 페이지

소책자 인쇄상 필요한 빈 면은 시스템만 처리한다.

- 편집 화면에 나타나지 않음
- 작품 페이지 수에 포함하지 않음
- 작가의 말 등으로 자동 활용하지 않음
- 마지막 본문과 뒤표지 사이에 위치

---

# 55. 현재 서재 필터는 유지한다

현재:

```text
모든 책
기본 이야기
내 작품
모두의 이야기
```

를 유지한다.

숏스토리만을 위한 별도 서재를 만들지 않는다.

---

# 56. 내 작품에서 형식만 표시한다

예:

```text
나의 별주부전
[숏스토리]
```

또는:

```text
나의 옹고집전
[놀스토리]
```

정도의 badge면 충분하다.

---

# 57. 현재 놀스토리 개발은 계속 유지한다

숏스토리 추가와 별개로 계속 발전시킨다.

- 놀스토리 각색 품질
- 분기 개연성
- 엔딩
- 원작 읽기
- Reader 가독성
- 저장·이어읽기
- 편집 편의
- 이미지 품질
- 모바일
- Excel
- `.knolstory`

숏스토리 개발 때문에 기존 놀스토리 개발이 멈추지 않도록 한다.

---

# 58. 개발 순서

불필요하게 많은 Phase를 만들지 않고 큰 단계만 둔다.

## 단계 1 — 서재 기본 작품 Action 정리

현재 DOM 삽입 방식의 `원작 읽기`를 정식 React 렌더링으로 바꾼다.

목표:

```text
원작 읽기
놀스토리 읽기
숏스토리
모두의 이야기 읽기
편집하기
```

를 정상적으로 구성할 기반 마련.

---

## 단계 2 — ShortStory 데이터 + Reader

- ShortStoryProject
- ShortStoryPage
- 별주부전 기본 프리셋
- 책으로 읽기
- 한눈에 보기

를 만든다.

먼저 읽기 경험을 완성한다.

---

## 단계 3 — ShortStoryEditor

- 글 수정
- 그림 변경
- 장면 추가
- 장면 삭제
- 복제
- 순서 이동

을 구현한다.

---

## 단계 4 — 로컬 저장 + `.shortstory`

- `storygame:shortstories:v1`
- 자동 저장
- 작업 파일 저장
- 작업 파일 불러오기

를 구현한다.

기존 놀스토리 저장소는 건드리지 않는다.

---

## 단계 5 — 서재·창작 관리 통합

- 내 작품에 숏스토리 표시
- 이어만들기
- 삭제
- 작업 파일 열기
- `.knolstory / .shortstory` 자동 판별

을 연결한다.

---

## 단계 6 — 숏스토리 Excel / Google 시트

- 별도 경량 Excel
- import/export
- 공개 Google 시트 읽기

를 추가한다.

---

## 단계 7 — 공유 HTML

먼저 숏스토리 HTML부터 만든다.

```text
.shortstory 작품
→ HTML 공유본
→ 파일 직접 열기
```

이 구조가 안정적으로 작동하면 놀스토리 HTML 공유를 추가한다.

---

## 단계 8 — 놀스토리 HTML 공유

현재 playback을 읽기 전용 payload로 변환한다.

- 선택
- 분기
- 엔딩
- 이미지
- 글상자

만 공유본에 필요한 수준으로 제공한다.

---

## 단계 9 — 활동지 / 책 인쇄

숏스토리 데이터에서:

- 활동지
- 책
- 소책자

출력을 추가한다.

---

## 단계 10 — 다른 작품 확대

별주부전에서 형식을 확정한 뒤:

```text
옹고집전
선녀와 나무꾼
```

순으로 숏스토리 기본 프리셋을 추가한다.

---

# 59. 검증을 과하게 하지 않는 원칙

이 프로젝트에서는 **검증을 많이 하는 것 자체를 품질로 보지 않는다.**

목적은:

> 기능을 안전하게 추가하면서 개발 속도와 집중력을 유지하는 것

이다.

---

# 60. 평소 작업의 기본 확인

일반적인 작은 작업은 다음 정도를 기본으로 한다.

```text
npm run check
+
변경한 기능 직접 1회 확인
```

관련된 작은 테스트가 이미 있으면 그것만 실행한다.

매번:

```text
npm test 전체
전체 browser smoke
모든 작품 전수 확인
```

을 하지 않는다.

---

# 61. 전체 테스트가 필요한 경우

다음처럼 공통 기반을 바꿨을 때만 전체 테스트를 우선한다.

- StoryProject schema
- `.knolstory` parser
- 공통 저장 repository
- StoryFlow
- 공통 Asset resolver
- Excel 공통 import/export
- 큰 화면 흐름 변경
- main 병합 직전의 큰 기능 묶음

---

# 62. 전체 테스트가 필요하지 않은 예

다음 작업에 매번 전체 놀스토리 회귀를 돌릴 필요는 없다.

- 숏스토리 페이지 버튼 배치
- 숏스토리 한눈에 보기 CSS
- 특정 숏스토리 프리셋 문장 수정
- 활동지 레이아웃 조정
- 숏스토리 전용 파일 parser 내부 수정
- 공유 HTML의 문구 수정

해당 기능만 확인하면 된다.

---

# 63. 원작 읽기 전수 컷 검사는 예외적으로만 한다

56컷·71컷·83컷을 매 변경마다 모두 넘겨 보는 방식은 사용하지 않는다.

전수 확인이 필요한 경우:

```text
원작 데이터 전체 순서 변경
원작 컷 연결 구조 변경
공통 Reader의 이동 로직 변경
```

정도다.

일반 디자인·자산 외부 기능 변경에는 대표 장면만 확인한다.

---

# 64. 화면 크기 검사도 필요한 때만 한다

항상:

```text
desktop
tablet
mobile
```

세 크기를 모두 확인하지 않는다.

UI 레이아웃을 실제로 바꿨을 때:

```text
주 화면 1개
+
필요하면 모바일 1개
```

정도를 확인한다.

반응형 자체를 수정했을 때만 여러 폭을 본다.

---

# 65. 실제 기기 검사

모든 개발 단계에서 실제 태블릿·휴대폰 검사를 하지 않는다.

다음처럼 기기 특성이 중요한 기능이 실제 완성됐을 때만 한다.

- 공유 HTML 파일 직접 열기
- 모바일 파일 선택
- 터치 중심 편집
- 인쇄
- 모바일 키보드 문제

---

# 66. 공유 HTML 확인도 단순하게 시작한다

초기에는:

```text
PC 주 브라우저 1개에서 파일 직접 열기
+
태블릿 1종에서 열기
```

정도로 확인한다.

지원 범위를 확대하면서 문제가 발견될 때 다른 브라우저를 추가한다.

---

# 67. 데이터 안전 관련 확인은 줄이지 않는다

검증을 간소화하더라도 다음은 확인한다.

- 기존 작품을 덮어쓰지 않는가
- 파일을 잘못 읽었을 때 현재 작품이 사라지지 않는가
- `.knolstory` 기존 파일을 계속 열 수 있는가
- `.shortstory` 저장 후 다시 열 수 있는가

이 부분은 실제 학생 작품 보존과 직접 연결되므로 우선한다.

---

# 68. 검증 실패를 이유로 불필요한 범위를 넓히지 않는다

문제가 하나 발견됐을 때:

```text
관련 영역 수정
→ 해당 기능 다시 확인
```

으로 끝낸다.

관련 없는 전체 프로젝트를 다시 조사하거나 대규모 리팩터링하지 않는다.

---

# 69. 문서에 검사 결과를 과도하게 누적하지 않는다

각 작업마다:

- 해상도별 모든 숫자
- 모든 컷 개수
- 수십 개 테스트 수
- 상세 스크린샷 경로
- 반복된 동일 회귀 결과

를 기획 문서에 계속 기록하지 않는다.

개발 상태 문서에는 필요한 증거만 간단히 남긴다.

예:

```text
npm run check 통과
핵심 사용자 흐름 확인
```

정도로 충분한 작업은 그렇게 기록한다.

---

# 70. 구현 완료 기준도 기능 중심으로 둔다

숏스토리 Reader라면:

```text
기본 작품에서 진입
→ 책으로 읽기
→ 한눈에 보기
→ 서재 복귀
```

가 되면 기능적으로 완료다.

테스트 개수가 많아야 완료인 것이 아니다.

---

# 71. 숏스토리 MVP 완료 기준

```text
기본 숏스토리 읽기
→ 직접 만들어 보기
→ 글/그림 수정
→ 페이지 추가·삭제·이동
→ 로컬 저장
→ .shortstory 저장
→ 다른 기기에서 다시 열어 편집
→ HTML로 친구에게 공유
```

가 가능하면 된다.

---

# 72. 놀스토리 HTML 공유 완료 기준

```text
놀스토리 작품
→ 플레이 적용
→ 친구에게 공유
→ HTML 생성
→ 친구가 HTML 열기
→ 선택·분기·엔딩 감상
```

이 되면 된다.

---

# 73. 예상 신규 파일

```text
app/shortstory/
  shortstory-data.ts
  shortstory-source.ts
  shortstory-commands.ts
  shortstory-repository.ts
  shortstory-file.ts
  shortstory-workbook.ts
  shortstory-sheet.ts
  shortstory-presets.ts

app/components/shortstory/
  ShortStoryWorkspace.tsx
  ShortStoryReader.tsx
  ShortStoryOverview.tsx
  ShortStoryEditor.tsx
  ShortStoryPageEditor.tsx
  ShortStoryWorksheetPrint.tsx
  ShortStoryBookPrint.tsx

app/share/
  share-payload.ts
  share-html-export.ts
```

필요해질 때만 파일을 추가한다.

---

# 74. 수정 가능성이 큰 기존 파일

```text
app/StoryStudio.tsx
app/components/StoryDiscovery.tsx
app/components/CreationHub.tsx
app/components/ClassicReadingLibraryEntry.tsx
app/story-discovery.ts
app/story-file.ts
app/components/StoryPlayer.tsx
app/story-assets.ts
app/story-asset-url.ts
```

한 번에 모두 수정하지 않는다.

각 단계에서 필요한 파일만 건드린다.

---

# 75. 개발 시 피해야 할 것

- 숏스토리를 StoryProject에 억지로 넣기
- StoryStudio에 숏스토리 조건문 계속 추가
- 기존 저장소 대규모 마이그레이션
- 숏스토리 때문에 기존 `.knolstory` 개편
- 모든 Reader를 하나로 합치기
- 모든 Editor를 하나로 합치기
- Share Runtime을 처음부터 거대한 범용 엔진으로 만들기
- 모든 단계마다 전체 회귀 실행
- 사소한 UI 변경마다 모든 기기 검사
- 원작 읽기 전 컷을 반복해서 전수 검사
- 검증을 위해 개발 범위를 계속 넓히기

---

# 76. 실제 권장 첫 작업

가장 자연스러운 첫 묶음은 다음이다.

```text
1. 서재 기본 작품 Action 정상화
2. ShortStoryProject 규격
3. 별주부전 ShortStoryReader
```

여기까지 한 번 완성한 뒤 Editor로 넘어간다.

공유 HTML 기술 확인은 ShortStory 기본 제작 흐름이 어느 정도 만들어진 시점에 작은 실험으로 진행하면 충분하다.

즉 **HTML 실험이 전체 숏스토리 개발의 선행 게이트는 아니다.**

---

# 77. 최종 파일 구조

```text
                      제작
                       │
        ┌──────────────┴──────────────┐
        │                             │
     놀스토리                       숏스토리
        │                             │
    .knolstory                   .shortstory
    작업 파일                     작업 파일
        │                             │
    다시 편집                     다시 편집
        │                             │
        └──────────────┬──────────────┘
                       │
                  친구에게 공유
                       ↓
                    작품.html
                       │
                   읽기 전용
                       │
             인터넷으로 공통 자원 로드
                       │
                 작품 바로 감상

                       +
                      PDF
                   인쇄 결과물
```

---

# 78. 최종 개발 원칙

1. **현재 놀스토리 구조와 사용자 작품을 우선 보존한다.**
2. **숏스토리는 별도 작품 형식으로 추가한다.**
3. **숏스토리는 별도 Reader와 Editor를 사용한다.**
4. **기존 놀스토리 저장소는 그대로 두고 숏스토리 저장소를 별도로 시작한다.**
5. **서재에서 두 형식을 자연스럽게 함께 보여준다.**
6. **`.knolstory`는 놀스토리 작업 파일이다.**
7. **`.shortstory`는 숏스토리 작업 파일이다.**
8. **`.html`은 친구에게 보내는 읽기 전용 공유본이다.**
9. **HTML을 열기 위해 스토리메이커 홈페이지에 들어가거나 업로드할 필요가 없다.**
10. **HTML은 공용 Reader·이미지·CSS만 인터넷에서 불러온다.**
11. **학생 HTML을 GitHub나 중앙 DB에 올릴 필요가 없다.**
12. **놀스토리 Excel과 숏스토리 Excel은 별도 규격으로 둔다.**
13. **활동지와 책 인쇄는 ShortStoryProject에서 직접 만든다.**
14. **검증은 변경 범위에 맞게 최소화한다.**
15. **학생 작품 보존·파일 호환처럼 데이터 안전과 직접 연결되는 확인은 유지한다.**
16. **사소한 작업마다 전체 회귀·전수 컷·전 기기 확인을 반복하지 않는다.**

---

# 79. 최종 정의

> **스토리메이커는 현재 놀스토리의 `StoryProject → StoryStudio → playback → StoryPlayer` 구조를 그대로 유지하면서, 별도의 `ShortStoryProject → ShortStoryEditor → ShortStoryReader` 형식을 추가한다. 놀스토리와 숏스토리의 작업 원본은 각각 `.knolstory`와 `.shortstory`로 저장하여 다시 편집할 수 있게 하고, 완성된 작품은 읽기 전용 `.html`로 친구에게 전달한다. HTML을 받은 사람은 별도 업로드나 사이트 탐색 없이 파일을 직접 열어 작품을 감상하며, 공통 Reader·이미지·CSS·연출만 현재 배포된 스토리메이커에서 인터넷으로 가져온다. 개발 과정의 검증은 변경한 기능과 데이터 안전에 필요한 수준으로 제한하고, 전체 회귀·전수 검사·다기기 검사는 실제 영향이 있는 변경이나 큰 통합 시점에만 수행한다.**
