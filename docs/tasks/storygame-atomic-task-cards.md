# storygame 원자 작업 카드

- 기준일: 2026-08-24
- 실행 규칙: `docs/storygame-ai-implementation-runbook.md`
- 현재 상태: `docs/storygame-development-status.md`
- 사용법: 상태표에서 유일한 `READY` ID의 카드만 실행한다.

모든 카드에 공통으로 `git status`와 대상 파일의 기존 diff 확인, 사용자 변경 보존,
`npm run check`, 최종 diff 검토가 적용된다. 아래 허용 파일은 새 파일을 포함하며,
금지 파일은 읽을 수 있지만 수정할 수 없다.

<a id="G0-01"></a>
## G0-01 — 작품 fixture 기반 고정

- 의존성: `OPS-01`
- 가치: 이전 작품과 손상 작품을 재현 가능하게 만들어 이후 저장 변경의 데이터
  손실을 막는다.
- 주 책임 / 검토: QA·릴리스 책임자 / 데이터 신뢰성 엔지니어
- 허용 파일: `package.json`, `tests/fixtures/story-projects.mjs`,
  `tests/story-project-fixtures.test.mjs`, 상태표
- 금지: `app/**`, Excel 원본·생성기, UI, 저장 키 변경, 새 의존성
- 사전 검사: `npm run check`; 허용 파일의 기존 diff 확인
- 구현:
  1. 현재 `StoryProject` 필드를 모두 가진 작고 고정된 v1 작품 fixture를 만든다.
  2. 정상본, `creativeMemos`가 없는 이전본, 중복 line ID, 없는 chapter를 참조하는
     line, 파싱 불가능 JSON 문자열을 각각 이름 붙여 제공한다.
  3. fixture를 매번 깊은 복사로 얻어 검사 간 변이를 막는다.
  4. fixture 자체의 정상·손상 특징과 고정 ID를 검사한다.
  5. `npm test`가 `tests/*.test.mjs` 전체를 실행하게 기존 스크립트만 최소 수정한다.
- 인수 조건:
  - 시간·무작위 값 없이 매번 같은 데이터가 나온다.
  - 각 손상 fixture는 의도한 결함 하나만 가진다.
  - 기존 5개 검사가 그대로 통과하고 새 fixture 검사가 추가된다.
  - 프로덕션 동작은 바뀌지 않는다.
- 검증: `node --test tests/story-project-fixtures.test.mjs`, `npm run check`, `npm test`
- 중단: 현재 타입만으로 정상 fixture를 판정할 수 없거나 허용 파일의 기존 변경과
  충돌하면 `BLOCKED`로 기록한다.
- 완료 상태: `G0-01 → DONE`, `G0-02 → READY`; 다른 작업은 시작하지 않는다.

<a id="G0-02"></a>
## G0-02 — 현재 v1 호환 동작 특성 검사

- 의존성: `G0-01`
- 가치: 마이그레이션 전의 실제 보존·결함 동작을 알아야 안전하게 개선할 수 있다.
- 주 책임 / 검토: QA·릴리스 책임자 / 데이터 신뢰성, 호환성 엔지니어
- 허용 파일: `tests/story-project-v1-compatibility.test.mjs`, 상태표
- 금지: `app/**`, 기존 테스트 약화, fixture 의미 변경
- 사전 검사: G0-01 검사와 `cloneProject`, `createBlankProject` 구현 확인
- 구현:
  1. 정상 v1과 `creativeMemos` 없는 v1을 현재 `cloneProject`에 통과시킨다.
  2. 입력 객체가 변하지 않고 작품의 줄바꿈·앞뒤 공백이 보존되는지 검사한다.
  3. 현재 구현이 중복 ID와 끊어진 참조를 고치지 못한다는 사실은 실패 검사가 아닌
     명시적 특성 검사 또는 테스트 설명으로 고정한다.
  4. 다음 마이그레이션 검사가 재사용할 수 있게 결함별 기대 결과를 분리한다.
- 인수 조건: 이전본은 안전한 기본값을 얻고 원문은 보존되며, 현재 미지원 검증
  동작이 거짓으로 성공 처리되지 않는다.
- 검증: `node --test tests/story-project-v1-compatibility.test.mjs`,
  `npm run check`, `npm test`
- 중단: 원하는 동작과 현재 동작이 달라 프로덕션 수정이 필요하면 결과를 기록하고
  G1-02로 넘긴다. 이 카드에서 앱 코드를 고치지 않는다.
- 완료 상태: `G0-02 → DONE`, `G0-03 → READY`.

<a id="G0-03"></a>
## G0-03 — 핵심 사용자 흐름 7개 실행 명세

- 의존성: `G0-02`
- 가치: 빌드 통과가 아닌 학생의 실제 성공 흐름을 같은 방식으로 반복 확인한다.
- 주 책임 / 검토: QA·릴리스 책임자 / 학생 대변인, 교육 UX 디자이너
- 허용 파일: `docs/qa/core-user-flows.md`, 상태표
- 금지: 프로덕션·테스트 코드, UI 수정
- 구현: 실행 계획의 7개 흐름마다 시작 데이터, 화면 크기, 번호가 있는 조작,
  초점·저장 상태·플레이 분리 기대 결과, 실패 증거 캡처 위치를 적는다. 자동화
  후보와 수동 전용 항목을 구분한다.
- 인수 조건: 다른 사람이 설명 없이 같은 절차를 반복할 수 있고, 빈 작품·이어쓰기·
  메모·Excel·오류·기기 복구·모바일 흐름이 모두 포함된다.
- 검증: 문서의 7개 고유 ID, 내부 링크, 파일 경로 검사
- 중단: 현재 UI에서 흐름을 시작할 방법 자체가 없으면 그 항목을 실패 사실로
  기록하되 UI를 고치지 않는다.
- 완료 상태: `G0-03 → DONE`, `G1-01 → READY`.

<a id="G1-01"></a>
## G1-01 — 버전이 있는 작품 문서 봉투

- 의존성: `G0-02`
- 가치: 저장 형식을 명시적으로 식별해 이전 작품을 안전하게 열 토대를 만든다.
- 주 책임 / 검토: 데이터 신뢰성 엔지니어 / 호환성, QA
- 허용 파일: `app/story-project-document.ts`,
  `tests/story-project-document.test.mjs`, 상태표
- 금지: `StoryStudio.tsx`, 기존 저장 키·UI·Excel 형식, `StoryProject` 필드 변경
- 구현: `CURRENT_SCHEMA_VERSION = 1`과 `{schemaVersion, savedAt, appVersion,
  project}` 문서 타입, 엄격한 ISO 시각 검사, 문서 생성 순수 함수를 만든다.
  `appVersion`은 호출자가 주입하며 브라우저 전역에 의존하지 않는다.
- 인수 조건: 원본 project를 변형하지 않고 문서를 만들며 잘못된 시각은 거부되고,
  결과를 JSON 왕복해도 작품 내용이 같다.
- 검증: 해당 단위 검사, `npm run check`, `npm test`
- 중단: 문서 형식을 Excel과 동시에 바꿔야 한다는 결론이 나면 구현하지 않고
  아키텍처 결정이 필요하다고 기록한다.
- 완료 상태: `G1-01 → DONE`, `G1-02 → READY`.

<a id="G1-02"></a>
## G1-02 — parse·normalize·validate·v1 마이그레이션

- 의존성: `G1-01`
- 가치: 빠진 선택 필드는 복원하되 손상본이 정상 작품을 덮어쓰지 않게 한다.
- 주 책임 / 검토: 데이터 신뢰성 엔지니어 / 호환성, QA
- 허용 파일: `app/story-project-document.ts`, 필요 시 새
  `app/story-project-validation.ts`, 관련 단위 검사, 상태표
- 금지: UI, localStorage 연결, Excel 변환, 자동으로 결함 ID 재작성
- 구현: unknown 입력을 판별하고, 봉투 없는 현재 v1을 마이그레이션하며, 선택 필드만
  기본값으로 채운다. 중복 ID·끊어진 chapter-line·잘못된 필수 타입은 위치가 있는
  오류로 반환한다. 예외 대신 명시적 성공/실패 결과를 사용한다.
- 인수 조건: G0 fixture 모두 기대대로 분류되고, 실패 입력이 부분 project를
  성공으로 돌려주지 않으며, 입력을 변형하지 않는다.
- 검증: 관련 단위 검사, `npm run check`, `npm test`
- 중단: 어떤 결함을 자동 복구할지 제품 결정이 필요하면 해당 결함은 복구하지 않고
  명시적 오류로 둔다.
- 완료 상태: `G1-02 → DONE`, `G1-03 → READY`.

<a id="G1-03"></a>
## G1-03 — 로컬 저장소 어댑터와 저장 상태

- 의존성: `G1-02`
- 가치: 빠르게 입력해도 마지막 작품이 저장되고 실패를 학생이 알 수 있다.
- 주 책임 / 검토: 데이터 신뢰성 엔지니어 / 학생 대변인, QA
- 허용 파일: 새 `app/story-project-repository.ts`, 관련 단위 검사,
  `app/StoryStudio.tsx`의 저장 호출부, 상태표
- 금지: 화면 재설계, 저장 키 삭제, 체크포인트 UI, 서버 저장
- 구현: 저장소 인터페이스와 localStorage 구현을 분리하고 기존 v1을 읽어 새 문서로
  저장한다. 300~800ms 지연 저장, flush, `idle/saving/saved/failed` 상태를 연결한다.
- 인수 조건: 연속 입력의 마지막 값 저장, 읽기 실패 시 현재 정상 draft 유지,
  저장 실패 안내와 Excel 보관 행동, active와 draft 상태 분리를 만족한다.
- 검증: fake storage/timer 단위 검사, `npm run check`, `npm test`, 핵심 흐름 1·6
- 중단: 기존 미커밋 StoryStudio 변경과 안전하게 분리할 수 없으면 중단한다.
- 완료 상태: `G1-03 → DONE`, `G1-04 → READY`.

<a id="G1-04"></a>
## G1-04 — 체크포인트·복구·삭제 되돌리기

- 의존성: `G1-03`
- 가치: 가져오기·플레이 반영·삭제 실수 뒤 작품을 되찾는다.
- 주 책임 / 검토: 데이터 신뢰성 엔지니어 / 교육 UX, QA
- 허용 파일: 저장소 모듈, 새 checkpoint 모듈과 검사, 복구·삭제 관련
  `StoryStudio.tsx` 부분, 관련 스타일, 상태표
- 금지: 클라우드 백업, 무제한 이력, 다른 편집 UI 개편
- 구현: 원인·시각·문서가 있는 체크포인트, 중복 방지, 최근 10개 기본 보존,
  가져오기·적용·삭제 전 생성, 복구 전 현재본 보존, 삭제 직후 undo를 구현한다.
- 인수 조건: 복구 취소와 복구 재실행 모두 현재본을 잃지 않고, 기기 삭제·교체는
  복구 불가임을 정확히 알리며, 용량 실패가 정상본을 덮어쓰지 않는다.
- 검증: checkpoint 단위 검사, `npm run check`, `npm test`, 흐름 5·6
- 완료 상태: `G1-04 → DONE`, `G2-01 → READY`.

<a id="G2-01"></a>
## G2-01 — 장면 변경 순수 명령

- 의존성: `G1-04`
- 가치: 장면 순서 변경과 삭제가 예측 가능하고 되돌릴 수 있다.
- 주 책임 / 검토: 프런트엔드 아키텍트 / 교육 UX, QA
- 허용 파일: 새 `app/story-commands.ts`, 단위 검사,
  `StoryStudio.tsx`의 장면 추가·삽입·이동·복제·삭제 호출부, 상태표
- 금지: 시각 변경, 저장 구조 변경, 챕터·메모 리팩터링
- 구현: 불변 순수 함수와 명시적 성공/실패 결과를 만든 뒤 기존 호출을 한 명령씩
  교체한다. ID 생성기는 주입한다.
- 인수 조건: 0·1·20개 장면, 처음·마지막 이동, 복제 ID, 삭제 후 선택 대상과
  order 연속성이 단위 검사로 증명된다.
- 검증: 명령 단위 검사, `npm run check`, `npm test`, 흐름 1·2
- 완료 상태: `G2-01 → DONE`, `G2-02 → READY`.

<a id="G2-02"></a>
## G2-02 — 장면·스크롤·초점 위치 복원

- 의존성: `G2-01`
- 가치: 대본과 장면 꾸미기를 오가도 학생이 쓰던 곳을 잃지 않는다.
- 주 책임 / 검토: 교육 UX 디자이너 / 접근성, 프런트엔드
- 허용 파일: 위치 상태용 새 모듈·검사, 관련 `StoryStudio.tsx` 부분과 스타일,
  상태표
- 금지: 화면 재배치, 저장·마이그레이션, 메모 기능 변경
- 구현: chapter/line/view/selection을 안정 ID로 저장하고 전환 뒤 대상이 있을 때만
  스크롤·초점을 복원한다. 새 장면은 본문으로 초점을 보낸다.
- 인수 조건: 대상 삭제 시 안전한 대체 위치, 20장면 왕복, 키보드 초점 표시,
  모바일 키보드에서 입력 가림 방지를 확인한다.
- 검증: 상태 단위 검사, `npm run check`, `npm test`, 390×844와 1440×900 흐름
- 완료 상태: `G2-02 → DONE`, `G2-03 → READY`.

<a id="G2-03"></a>
## G2-03 — 오류 위치 이동과 강조

- 의존성: `G2-02`
- 가치: 학생이 오류를 읽고 한 번에 고칠 입력으로 이동한다.
- 주 책임 / 검토: 교육 UX 디자이너 / 학생 대변인, 접근성, QA
- 허용 파일: validation issue 모듈·검사, 적용 오류 UI 관련 컴포넌트·스타일,
  상태표
- 금지: 구상 선택 필드의 필수화, Excel 오류 통합, 자동 문장 수정
- 구현: `{code,message,chapterId?,lineId?,field}` 문제 모델과 이동 명령을 만들고
  오류 선택 시 화면·입력·초점·일시 강조를 연결한다.
- 인수 조건: 오류 문구에 위치와 행동이 있고, 색만으로 표시하지 않으며, 선택적
  고쳐쓰기 제안은 플레이 차단 오류와 분리된다.
- 검증: issue 단위 검사, `npm run check`, `npm test`, 키보드 흐름 1
- 완료 상태: `G2-03 → DONE`, `G2-04 → READY`.

<a id="G2-04"></a>
## G2-04 — 창작 메모 왕복과 되돌리기

- 의존성: `G2-03`
- 가치: 아이디어를 기록한 뒤 쓰던 장면으로 정확히 돌아간다.
- 주 책임 / 검토: 초등 국어 교사 / 교육 UX, 접근성
- 허용 파일: creative memo 명령·검사, 관련 `StoryStudio.tsx`와 스타일, 상태표
- 금지: 플레이에 메모 노출, 자동 본문 삽입, 메모 종류 추가
- 구현: 연결 chapter/line 선택, 열기 전 위치 기억, 검색 결과→수정→원위치 복귀,
  삭제 undo, 모바일의 명확한 돌아가기를 연결한다.
- 인수 조건: 메모는 플레이에 나오지 않고, 끊어진 연결은 안전하게 표시되며,
  원래 대상이 삭제되면 가장 가까운 안전 위치로 돌아간다.
- 검증: 메모 명령 검사, `npm run check`, `npm test`, 흐름 3·7
- 완료 상태: `G2-04 → DONE`, `G2-05 → READY`.

<a id="G2-05"></a>
## G2-05 — 비채점형 고쳐쓰기 순환

- 의존성: `G2-04`
- 가치: 플레이 결과를 보고 인물·사건·결말을 스스로 점검한다.
- 주 책임 / 검토: 초등 국어 교사 / 작가·편집자, 학생 대변인
- 허용 파일: 고쳐쓰기 데이터/컴포넌트/스타일과 검사, 플레이 종료·대본 하단
  연결부, 상태표
- 금지: 점수·등급, 자동 생성·자동 삽입, 플레이 차단
- 구현: 구조 모드에 맞는 짧은 질문, `확인함/나중에`, 편집으로 돌아가기와 원래
  장면 복원을 구현한다.
- 인수 조건: 질문은 하나의 정답을 요구하지 않고, 저장하지 않아도 플레이 가능하며,
  학생 작품 본문을 분석·전송하지 않는다.
- 검증: 콘텐츠 데이터 검사, `npm run check`, `npm test`, 흐름 1·2
- 완료 상태: `G2-05 → DONE`, `G3-01 → READY`.

<a id="G3-01"></a>
## G3-01 — 화면 상태와 작품 상태 분리

- 의존성: `G2-05`
- 가치: 기능 변경이 다른 화면과 작품 데이터를 우연히 깨뜨릴 위험을 줄인다.
- 주 책임 / 검토: 프런트엔드 아키텍트 / QA, 교육 UX
- 허용 파일: 새 state/reducer/selector 모듈과 검사, 단계적으로 교체하는
  `StoryStudio.tsx` 부분, 상태표
- 금지: 전면 재작성, 시각 변경, 저장·외부 변환 변경
- 구현: 작품 명령, 임시 UI 상태, selector를 분리하고 한 상태 묶음씩 기존 호출을
  교체한다. 한 번에 한 화면을 넘지 않는다.
- 인수 조건: 핵심 명령이 React 없이 검사되고, 기존 흐름·문구·DOM 구조가 유지되며,
  StoryStudio 변경이 조립 책임 축소로 설명 가능하다.
- 검증: reducer/selector 검사, `npm run check`, `npm test`, 영향 흐름
- 완료 상태: `G3-01 → DONE`, `G3-02 → READY`.

<a id="G3-02"></a>
## G3-02 — Excel·시트 어댑터와 공통 오류 모델

- 의존성: `G3-01`
- 가치: 가져오기 실패가 작품을 훼손하지 않고 문제 위치를 정확히 알려 준다.
- 주 책임 / 검토: 호환성 엔지니어 / 데이터 신뢰성, 교실 운영, QA
- 허용 파일: `story-workbook.ts`, `story-sheet.ts`, 새 오류/어댑터 모듈,
  fixture와 검사, 관련 호출부, 상태표
- 금지: 공식 탭·열의 무계획 변경, 본문 trim, UI 재설계
- 구현: 공통 snapshot과 `{severity,source,sheet,row,column,value,message,fix}` 오류를
  분리한다. 원문과 판정용 정규화 값을 따로 보존한다.
- 인수 조건: 최신·이전·공백·줄바꿈·쉼표·따옴표·손상 fixture가 기대대로 왕복하고,
  실패 시 현재 draft가 유지된다.
- 검증: import/export 검사, `npm run check`, `npm test`, 흐름 4·5
- 완료 상태: `G3-02 → DONE`, `G3-03 → READY`.

<a id="G3-03"></a>
## G3-03 — 화면·Dialog·CSS의 점진 분리

- 의존성: `G3-02`
- 가치: 화면 하나를 고칠 때 다른 화면과 반응형 규칙을 건드리지 않게 한다.
- 주 책임 / 검토: 프런트엔드 아키텍트 / 교육 UX, 접근성, QA
- 허용 파일: 새 components/styles 모듈, 한 번에 분리할 한 컴포넌트 관련
  `StoryStudio.tsx`·`globals.css`, 상태표
- 금지: 한 카드에서 여러 화면 분리, 시각 재설계, 토큰 변경
- 구현: 공통 Dialog→Asset Picker→시작→구상→대본→장면→플레이 순서를 지키되,
  이 카드 실행 세션에서는 첫 미분리 대상 하나만 옮긴다. 나머지는 후속 하위 ID로
  쪼갠다.
- 인수 조건: DOM·문구·키보드·반응형 결과가 유지되고, props와 상태 책임이 명확하며,
  12px 미만 필수 문구와 44px 미만 조작을 새로 만들지 않는다.
- 검증: `npm run check`, `npm test`, 영향 화면 390×844·1440×900
- 완료 상태: 모든 하위 분리 카드가 끝난 뒤에만 `G3-03 → DONE`, `G3-04 → READY`.

<a id="G3-04"></a>
## G3-04 — 핵심 상호작용 자동 검사

- 의존성: `G3-03`, `G0-03`
- 가치: 실제 상태 전이 회귀를 배포 전에 발견한다.
- 주 책임 / 검토: QA·릴리스 책임자 / 프런트엔드, 학생 대변인
- 허용 파일: 테스트 도구 설정, `tests/**`, 테스트용 최소 selector 속성, 상태표
- 금지: 테스트를 위한 제품 동작 변경, 모든 흐름의 한 번에 자동화, 불안정 sleep
- 구현: 기존 도구로 가능한지 먼저 확인하고, 핵심 흐름 1부터 한 흐름씩 독립 카드로
  자동화한다. 역할·이름·안정 ID를 우선 사용한다.
- 인수 조건: 실패 시 학생 행동 단계가 보이고, 저장·초점·active/draft 분리를
  검증하며, 반복 실행에서 안정적이다.
- 검증: 새 상호작용 검사 3회 반복, `npm run check`, `npm test`
- 완료 상태: 7개 흐름 자동/반복 검사 체계가 모두 있으면 `G3-04 → DONE`,
  `G4-01 → READY`.

<a id="G4-01"></a>
## G4-01 — 가져오기 오류와 미리보기

- 의존성: `G3-04`
- 가치: 학생이 현재 작품을 덮기 전에 무엇이 바뀌고 어디가 틀렸는지 안다.
- 주 책임 / 검토: 호환성 엔지니어 / 교육 UX, 데이터 신뢰성, 교실 운영
- 허용 파일: import preview 컴포넌트·스타일·검사, 어댑터 오류 표시 호출부, 상태표
- 금지: 형식 자동 수정, 현재 작품 즉시 덮어쓰기, Google 인증
- 구현: 치명 오류/경고, 탭·행·열·현재값·고치는 법, 챕터·장면·메모 수와 적용 전
  확인, 취소를 구현한다. 공개 시트 오류에는 공개 설정과 Excel 대체 경로를 준다.
- 인수 조건: 취소·실패 시 draft 유지, 키보드 가능한 Dialog, 긴 오류 목록 탐색을
  만족한다.
- 검증: 컴포넌트/어댑터 검사, `npm run check`, `npm test`, 흐름 4·5
- 완료 상태: `G4-01 → DONE`, `G4-02 → READY`.

<a id="G4-02"></a>
## G4-02 — 이미지·번들·오프라인 성능

- 의존성: `G4-01`
- 가치: 저사양 태블릿과 불안정한 교실 네트워크에서도 편집·플레이가 이어진다.
- 주 책임 / 검토: 프런트엔드 아키텍트 / 미디어 자산, 교실 운영, QA
- 허용 파일: 자산 생성기·카탈로그·로딩 코드·캐시 설정·성능 기록, 상태표
- 금지: 측정 없는 Worker/서비스 추가, 원본 삭제, 작품 캐시와 앱 캐시 혼합
- 구현: 먼저 번들·첫 로드·긴 작품·이미지 크기를 측정한다. 가장 큰 병목 하나만
  카드로 해결하고 재측정한다. 썸네일은 생성기를 통해 만든다.
- 인수 조건: 수치가 전후로 기록되고, 화면 밖 지연 로딩·현재 챕터 우선·첫 로드 후
  로컬 편집과 플레이가 유지되며, 작품 데이터가 캐시 갱신에 삭제되지 않는다.
- 검증: 생성기 재현성, `npm run check`, `npm test`, 성능 시나리오
- 완료 상태: 목표 기준을 충족한 뒤 `G4-02 → DONE`, `G4-03 → READY`.

<a id="G4-03"></a>
## G4-03 — 접근성 마감

- 의존성: `G4-02`
- 가치: 키보드·저시력·터치 사용자도 핵심 제작 흐름을 끝낸다.
- 주 책임 / 검토: 접근성 검토자 / 교육 UX, 학생 대변인, QA
- 허용 파일: 공통 Dialog/조작 컴포넌트·스타일·접근성 검사, 상태표
- 금지: 시각 전면 재설계, 색만으로 상태 표시, 핵심 정보 축소
- 구현: focus trap/복귀, Escape, 44px, 12px 이상 필수 문구, 대비, 200% 확대,
  움직임 감소, 플레이 키보드 이동을 항목별 하위 카드로 해결한다.
- 인수 조건: 키보드만으로 빈 작품→첫 플레이, 모달 초점 이탈 없음, 확대 시 내용
  손실 없음, 모바일 키보드에서 입력과 주요 조작이 보인다.
- 검증: 자동 접근성 검사와 수동 키보드 체크리스트, `npm run check`, `npm test`,
  390×844·768×1024·1024×768·1440×900
- 완료 상태: 모든 항목 증거 뒤 `G4-03 → DONE`, `G4-04 → READY`.

<a id="G4-04"></a>
## G4-04 — 교실 파일럿과 상위 문제 3개

- 의존성: `G4-03`
- 가치: 실제 학생과 교사가 반복 수업에서 막히지 않는지 확인한다.
- 주 책임 / 검토: 교실 운영 교사 / 제품 책임자, 접근성, 개인정보, QA
- 허용 파일: 익명 파일럿 계획·관찰표·집계 결과·후속 작업 상태, 상태표
- 금지: 학생 작품 본문·이름 기본 수집, 동의 없는 녹화, 한 번에 4개 이상 개선
- 구현: 처음 사용자·이어쓰기·Excel 복구 시나리오를 학생 5명 이상에게 관찰하고,
  첫 플레이 시간·교사 개입·막힘 유형만 최소 수집한다. P0와 빈도 상위 3개만 새
  원자 작업으로 만든다.
- 인수 조건: 개인정보 없는 집계, 재현 단계가 있는 문제, P0 즉시 중단 기준,
  반복 파일럿의 비교 기준이 있다.
- 검증: 동의·최소수집 점검, 집계 계산 검토, 생성된 후속 카드의 완전성 검사
- 완료 상태: P0가 없고 상위 3개 후속 작업이 처리된 반복 수업 뒤 `G4-04 → DONE`.
