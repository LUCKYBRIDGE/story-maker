# AGENTS.md — storygame 개발 운영 헌장

When this checkout is inside WAN2, read `/Volumes/WAN2/AGENTS.md` once if not already loaded. Outside WAN2, use this repository's own rules without requiring that drive.

> Project: `storygame`
> Canonical Shared Source: GitHub `LUCKYBRIDGE/story-maker`의 `main`과 현재 작업 PR
> Local Execution Copy: `/Volumes/WAN2/apps/story-maker`
> Product: 로그인 없이 기기 안에서 쓰고, Excel로 보관하며, 플레이로 고쳐 쓰는 학생용 스토리게임 창작 도구

## 1. 빠른 시작 — 최소 문맥 원칙

AI/Work는 저장소 전체를 먼저 읽지 않는다. 다음 순서로 필요한 정보만 확장한다.

1. **Work/로컬**: `npm run status:check` 실행 후 현재 branch, HEAD, dirty files, 열린 PR만 확인한다.
   **GitHub Chat**: 실제 `main`, 작업 branch/PR, 필요한 상태 파일만 GitHub에서 확인한다.
2. `STATUS.md`는 상태가 모호하거나 기준선을 확인할 때만 읽는다.
3. `docs/storygame-development-status.md`에서 현재 `READY`/`IN_PROGRESS`와 직접 연결된 항목만 읽는다.
4. 해당 작업 카드와 영향받는 소스/테스트만 읽는다.
5. 설계·ADR·목업은 현재 결정이 필요한 절만 추가로 읽는다. 이미 로드한 동일 문서는 반복해서 읽지 않는다.

**금지:** 세션 시작마다 장기 로드맵, 전체 상태 이력, 모든 작업 카드, 모든 ADR을 순회하지 않는다.

## 2. Source of Truth와 충돌 해결

충돌 시 우선순위는 다음과 같다.

1. 사용자가 현재 대화에서 명시한 결정
2. 실제 프로젝트 소스와 공식 Excel 양식
3. `docs/storygame-detailed-design.md` — 제품 계약
4. `docs/storygame-development-status.md` — 현재 실행 상태
5. 해당 원자 작업 카드 — 작업 범위·인수 조건
6. `docs/storygame-ai-implementation-runbook.md` — 실행·중단 절차
7. `docs/operations/github-first-hybrid-development.md` — GitHub/Chat/Work 운영 계약
8. 관련 ADR·디자인 문서
9. 장기 계획·로드맵

구현 사실은 코드/실행으로, 제품 의도는 승인된 제품 계약으로 판단한다. 같은 현재 상태를 여러 문서에 복사하지 않는다. 지속할 아키텍처 결정만 `docs/decisions/`에 ADR로 남긴다.

## 3. 실행 모델 — 기본은 계속 진행

작업은 **한 번에 하나만 `IN_PROGRESS`**로 유지하지만, 세션은 한 작업에서 끝낼 필요가 없다.

- 사용자가 구현·정비를 맡겼고 목표가 명확하면, 현재 작업을 `DONE` 처리한 뒤 **다음 의존 작업을 자동으로 `READY → IN_PROGRESS`로 전환하여 같은 세션에서 계속 진행**한다.
- 하나의 milestone/workstream 안에서 선행 관계가 명확한 원자 작업들은 순차적으로 이어서 수행한다.
- 매 작업마다 사용자 확인을 요구하지 않는다. 필요한 경우에만 짧은 체크포인트를 남기고 계속한다.
- 별도 계획 파일이나 반복적인 중간 보고를 만들지 않는다. 상태표에는 현재 상태와 완료 증거만 짧게 남긴다.

### 반드시 멈추는 경우

다음 중 하나일 때만 사용자 판단을 기다린다.

- 데이터 손실, 호환성 파괴, 인증·공개 범위·비용 등 결과가 크게 달라지는 결정
- `main` merge, 공개 배포, force push, 비밀정보/권한 변경
- 요구가 서로 충돌하여 기존 제품 계약만으로 안전하게 결정할 수 없음
- 다른 사람의 미커밋/미푸시 작업과 충돌하여 보존할 방법이 불명확함
- 같은 원인의 수정이 3회 실패함
- 다음 작업이 실제로 BLOCKED이며 우회가 제품 요구를 바꿈

## 4. GitHub-first 작업 원칙

- `main`은 승인·통합된 공유 기준선이다. 직접 개발하지 않는다.
- 구현은 작업 branch/PR에서 한다. commit·push·draft PR 같은 되돌릴 수 있는 작업은 자율적으로 수행할 수 있다.
- Work/로컬 변경은 장시간 로컬에만 두지 않고 의미 있는 체크포인트에서 같은 branch/PR로 동기화한다.
- Chat과 Work가 같은 파일을 동시에 수정하지 않는다.
- 로컬 dirty files는 사용자 작업으로 보존한다. 지우거나 덮어쓰지 않는다.
- GitHub와 로컬이 다르면 숨기지 말고 어느 쪽이 기준인지 확인한 뒤 진행한다.

주도권은 작업 특성으로 고른다.

- **Chat Lead**: GitHub 안의 로직·데이터·문서·테스트·PR/CI 판단 중심
- **Work Lead**: 실제 브라우저·런타임·로컬 파일·자산·성능·OS/하드웨어 피드백 중심
- **Joint**: 계약/테스트와 실제 환경 반복 구현이 모두 중요한 경우

## 5. 명령과 검증 비용

### 기본 명령

- 빠른 상태: `npm run status:check`
- 개발: `npm run dev`
- 정적 검사: `npm run check`
- 빌드: `npm run build`
- 전체 회귀: `npm test`
- 브라우저 smoke: `npm run qa:smoke`
- 교실 QA: `npm run qa:classroom`
- 공식 Excel: `npm run template:generate`

### 최소 검증 사다리

검증은 **가장 가까운 증거부터 시작하고, 실패 위험이나 변경 범위가 커질 때만 넓힌다.**

1. **문서/지침만 변경**: 링크·경로·중복·충돌 확인. 코드 build/test는 실행하지 않는다.
2. **국소 로직/데이터 변경**: 영향받는 단위/회귀 테스트 + 필요한 정적 검사만 실행한다.
3. **UI 변경**: 관련 자동 검사 + 대표 viewport의 실제 흐름만 확인한다. 모든 화면을 반복 순회하지 않는다.
4. **저장/Excel/스키마/마이그레이션/공유 런타임 변경**: 호환 fixture와 관련 회귀를 추가한다.
5. **릴리스 후보, 의존성/빌드 체계 변경, 폭넓은 공통 코드 변경**: `npm test`와 필요한 smoke를 실행한다.

CI가 이미 같은 commit에서 성공한 동일 명령은 Work가 다시 실행하지 않는다. 로컬/OS 차이 또는 실제 runtime 검증이 필요한 경우에만 반복한다.

**검증을 위한 검증 금지:** unrelated 전체 회귀, 모든 브라우저/viewport 반복, 이미 통과한 동일 명령 재실행, 현재 변경과 무관한 과거 QA 재수행을 기본 절차로 두지 않는다.

## 6. 변경 안전 규칙

- 가장 작은 **완결 단위**로 구현하되, 완료 후 다음 관련 작업으로 계속 진행할 수 있다.
- 기존 컴포넌트, 데이터 변환 함수, 디자인 토큰을 먼저 재사용한다.
- 학생 작품 보존과 이전 저장본/Excel 호환은 새 기능보다 우선한다.
- 편집본/플레이 버전, 화자 이름/이미지, 창작 메모/플레이 문장을 분리한다.
- 로그인, 네트워크, 외부 API를 핵심 제작 흐름의 새 필수 조건으로 만들지 않는다.
- 새 의존성은 기존 도구로 해결할 수 없고 제품 가치가 명확할 때만 추가한다.
- UI 전면 재작성은 하지 않는다. 현재 흐름을 보존하며 단계적으로 수정한다.
- 생성 파일은 원본/생성기를 먼저 수정한다.

### 캐릭터/이미지

이미지를 생성·보정·교체할 때는 `docs/decisions/character-pose-normalization-v1.md`의 관련 절을 읽는다. 기준본·색감·머리/몸 비율을 고정하고, 발선/위치는 평행이동 또는 균일 확대·축소로 먼저 해결한다. 비균일 확대와 머리만 따로 바꾸는 보정은 금지한다.

### U1 화면

U1 관련 변경에서만 `docs/decisions/studio-screen-flow-v1.md`와 `docs/design/mockups/README.md`의 관련 절을 확인한다. 메인 A의 `이어만들기`, 독립 이야기 구성, 대본/컷 두 보기, 선형 플레이를 유지하며 목업의 숫자·잘린 인물·중복 버튼을 사양으로 복사하지 않는다.

## 7. 품질 관점 — 필요한 것만 적용

역할극 문서나 별도 검토표를 만들지 않는다. 현재 변경에 해당하는 질문만 확인한다.

| 변경 | 핵심 질문 |
|---|---|
| 제품 범위 | 학생의 첫 성공과 고쳐 쓰기에 직접 도움이 되는가? |
| 이야기/문구 | 생각을 대신 쓰지 않고 짧고 구체적으로 다음 행동을 돕는가? |
| UI | 글쓰기가 첫눈에 보이고 터치·키보드로 가능한가? |
| 저장/마이그레이션 | 실패해도 작품을 되찾고 이전 형식을 열 수 있는가? |
| Excel/시트 | 문제 위치와 수정 방법이 드러나는가? |
| 이미지 | 기준본·색감·비율·발선이 일관되는가? |
| 테스트/릴리스 | 실제 사용자 흐름을 필요한 수준으로 증명했는가? |

학생 작품 보존, 개인정보, 접근성은 편의 때문에 생략하지 않는다.

## 8. 완료와 연속 실행

하나의 원자 작업은 다음이면 `DONE`이다.

- 사용자 이야기와 인수 조건 충족
- 관련 호환성 보존
- 최소 필요한 자동/실제 흐름 검증 통과
- 최종 diff에 무관한 변경·비밀정보·불필요한 생성물이 없음
- 미검증 항목이 있으면 이유가 명시됨

`DONE` 뒤에는 다음을 수행한다.

1. 상태표에 핵심 결과와 **실제로 실행한 검증만** 한두 줄로 기록한다.
2. 다음 의존 작업이 명확하고 안전하면 바로 `READY → IN_PROGRESS`로 넘겨 계속한다.
3. milestone이 끝났거나 중단 조건이 발생했을 때만 사용자에게 완료/판단 요청을 보고한다.

## 9. 현재 범위 경계

명시적 제품 결정 전까지 기본 범위가 아니다.

- 학생 회원가입과 자체 작품 서버
- 여러 기기 실시간 공동 편집
- Google OAuth와 시트 자동 저장
- 학생용 AI 글·이미지 자동 생성
- 공개 작품 커뮤니티
- 점수·조건에 따른 선택지 잠금과 무한 반복형 플레이

2026-09-09 사용자 결정으로 선택지 2/3개와 컷 연결·분기·합류는 BR-01 구현 범위다. 기존 선형 작품 호환은 계속 보존한다.
